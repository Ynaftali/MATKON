# פרומט לסשן חדש: הקמת סביבת בדיקה ופיתוח

> העתק את כל הבלוק שמתחת לקו לסשן חדש.
> **המודל: Sonnet 5** (`claude-sonnet-5`). לא Opus ולא Fable.
> זו משימת הרכבה (התקנה, חיבור מפתחות, יצירת עותק מסד, אימות), לא משימת חשיבה. אין כאן החלטה ארכיטקטונית פתוחה ולא באג תקוע, שזה מה שמצדיק מודל כבד.
> **אל תוסיף לפרומט בקשה "לחשוב לעומק" / "ultrathink".** הטקסט שלמטה נוסח בלי הטריגרים האלה בכוונה: חשיבה מורחבת כאן שורפת תקציב בלי תמורה. אם נתקעים על משהו אמיתי באמצע, אז ורק אז מעלים מודל.

---

בוקר טוב, ממשיכים ב-matkon. Branch: `ux-redesign-preview` | Working dir: `/Users/naftali/claudecode/israelkitchen`

**הכנה (חובה):** קרא במלואם את המזכר `project_e2e_fix_batch` ואת `mission/tasks_2026-07-18.md`.

## המשימה: להקים סביבת בדיקה ופיתוח

**למה זו המשימה החוסמת.** היום אין שום דרך לבדוק שינוי בשרת או במסד לפני שהוא חי אצל משתמשים. ב-18.7 זה עלה לנו במחיר ממשי, ארבע פעמים ביום אחד:

1. מיגרציה שהוחלה ישירות על הפרודקשן **שברה קריאת מתכונים לכל גולש לא מחובר**. היא התגלתה במקרה, ורק כי הבאג הוסתר על ידי fallback שעוקף הרשאות.
2. ניתוב לפי סוג דפדפן (תצוגה מקדימה בוואטסאפ) לא היה ניתן לבדיקה לפני פריסה.
3. בדיקת פונקציות שרת חייבה "לזייף" את המסד, כי אין מפתח שרת בסביבה המקומית.
4. אימות מסלול התחברות נעצר באמצע, כי אין משתמש בדיקה בסביבה נפרדת.

**הדפוס: בודקים אחרי הפריסה במקום לפניה.** המשימה הזו נועדה להפוך את זה.

## תנאי סף לבדוק בהתחלה

| # | מה | סטטוס ידוע |
|---|---|---|
| 1 | כלי ה-Vercel (CLI) מותקן? | **לא מותקן** נכון ל-18.7 |
| 2 | מפתח שרת (service role) בקובץ הגדרות מקומי | **חסר.** `.env` מכיל רק `VITE_SUPABASE_URL` ו-`VITE_SUPABASE_ANON_KEY` |
| 3 | עותק מסד לבדיקות (Supabase branch) | **לא קיים, ועולה כסף** |

## קריטריון "בוצע"

הסביבה נחשבת מוקמת כשכל אלה מתקיימים:

1. `/api/*` עונה מקומית תשובת אמת (לא `index.html` של האפליקציה)
2. אפשר להריץ מיגרציה על מסד בדיקות **בלי לגעת בפרודקשן**
3. אפשר לאמת שינוי RLS במסלול **אנונימי** ישירות מול המסד
4. אפשר לבדוק ניתוב לפי סוג דפדפן (crawler מול גולש) מקומית
5. יש משתמש בדיקה שאפשר להתחבר איתו בלי להשתמש בחשבון האמיתי
6. מתועד ב-`CLAUDE.md`: איך מריצים ומה עושים לפני כל שינוי שרת/מסד

## החלטות שדורשות אישור המשתמש לפני ביצוע

**אל תבצע אותן לבד. שאל, וחכה לתשובה.**

1. **עלות עותק המסד.** לברר את המחיר המדויק, להציג אותו, ולשאול: עותק קבוע או זמני שמוקם לפי צורך? המשתמש כבר משלם על Supabase Pro ועל Vercel Pro, והוא מקפיד לדעת עלויות מראש.
2. **הפניית קריאות מקומיות לשרת אמיתי.** זו דרך מפתה לפתור את בעיה 1, ו**מסוכנת**: בדיקה מקומית תוכל לשנות נתוני אמת. עלה כשיקול ב-18.7 והוחלט שזו החלטת תכנון למשימה הזו. להציג בעד ונגד ולתת המלצה.
3. **משתמש בדיקה.** יצירת חשבון דורשת סיסמה, והסוכן לא מזין סיסמאות. להציע דרך שבה המשתמש עצמו יוצר אותו, או ליצור אותו ישירות במסד הבדיקות.

## כללי עבודה (מחייבים)

- **הסבר קצר בעברית לפני כל צעד.** המשתמש אינו מפתח, בלי מונחים באנגלית.
- **בלי em dash.**
- **רשימות משימות תמיד כטבלה.**
- **פעולות ידניות בלוח בקרה: צעד אחד בכל פעם**, ולחכות לאישור. לא רשימה של חמישה צעדים.
- **כל שינוי ויזואלי: צילום מסביבת בדיקה אמיתית, לא הדמיה.** אם מציגים כמה אופציות, **כולן בו-זמנית על צילום אחד ומסומנות**, אף פעם לא אחת אחרי השנייה.
- לפני פריסה: לוודא `grep TEMP-PREVIEW` ו-`grep TEMP-COMPARE` מחזירים 0.
- בדיקת האיכות (`npx eslint src/ api/`) עומדת על **73 בעיות** בבסיס. כל מספר גבוה יותר = רגרסיה שהוספת.

## מצב הקוד

- **פרוס וחי** על matkon.co: הכל עד commit `3645a1b`.
- **מקומיט מקומית וטרם נדחף:** `fdea196` (K8, חותמת "MADE WITH AI"). הסגנון אושר עקרונית אך **טרם נראה על מסך**. לפני פריסה: צילום מעל תצלום כהה **וגם** בהיר, כי הסיכון הוא טקסט לבן בשקיפות 50% שנעלם על אוכל בהיר.
- rollback בטוח: `0e4ae31`.
- זהות: team `team_H42GJVHlmoZpq6pQRtXnwGPo`, project `prj_u347kw0yBqJd6isoWNJVwcykRKnw`, Supabase project `txstregguvnuyyxensun`.

## באג פתוח שהתגלה 18.7 — לא לתקן עכשיו, לתקן מיד אחרי הקמת הסביבה

התצוגה המקדימה בוואטסאפ נכשלת ב**שליחה הראשונה** של כל מתכון חדש. אומת במדידה: בקשה ראשונה ל-`/api/og-image` = **2.66 שניות** (יצירת התמונה מאפס), בקשה שנייה = **0.16 שניות** (מטמון). וואטסאפ לא ממתין ומוותר, ולכן **הנמען הראשון מקבל קישור עירום** — כלומר נפגע דווקא הרושם הראשון.

כיווני פתרון: לחמם את התמונה ברגע הלחיצה על כפתור השיתוף, או לשמור את העותק המוקטן ב-Storage במקום לייצר בכל בקשה. זה תיקון בשרת, ולכן הוא בדיוק המקרה שכדאי לעשות עם סביבת הבדיקה החדשה כרשת ביטחון.

## אומת ונסגר 18.7 (אין צורך לבדוק שוב)

- שיתוף בוואטסאפ: הכרטיס מציג תמונה + "קיבלת MATKON" + שם המתכון, והתיאור הארוך ירד מגוף ההודעה.
- חזרה למתכון אחרי התחברות: אורח → פופאפ → כניסה → נוחת בחזרה על אותו מתכון. עובד.
- ⚠️ לידיעה: וואטסאפ פותח קישורים בדפדפן פנימי משלו, עם זיכרון התחברות **נפרד** מספארי. יציאה מהחשבון בספארי לא מוציאה משם. זו אינה תקלה, אבל היא מבלבלת בבדיקות ידניות.

**התחל בבדיקת שלושת תנאי הסף, ואז הצג לי טבלת צעדים לאישור לפני שאתה מבצע משהו.**
