# MATKON — יומן שינויים והחלטות

יומן כרונולוגי של **כל** השינויים, התיקונים, ההחלטות והדברים שנדחו לעתיד — בנוסף למסמכי ה-`Brief/`.
המטרה: שגם אדם מבחוץ (לא בהכרח טכני) יבין **מה נעשה, מתי, מה שמנו בצד ולמה**.

- **מקור אמת:** הקובץ הזה (טקסט, מנוהל בגרסאות עם הקוד ב-git).
- **ייצוא ל-Excel:** לפי בקשה — אפשר לייצר `.xlsx` מהטבלה הזו (אותן עמודות) לשיתוף/סינון.
- **מתי מעדכנים:** בסוף כל סשן (כשהמשתמש אומר "סיימנו").
- **עמודות:** תאריך · סוג · תחום · מה נעשה / הוחלט · למה / הערות · סטטוס.
- **סוג:** ✨ תוספת · 🐛 תיקון · 🔒 אבטחה · 🧭 החלטה · ⏳ נדחה לעתיד.
- **סטטוס:** פרוס · בוצע · ממתין · פתוח · נדחה.

> השורות מסודרות מהחדש לישן.

| תאריך | סוג | תחום | מה נעשה / הוחלט | למה / הערות | סטטוס |
|-------|-----|------|----------------|-------------|-------|
| 2026-07-23 | 🐛 | חשבון/PWA | **ההתחברות עברה משמירה ב-localStorage לשמירה בעוגייה עמידה (תוקף שנה), כדי שאייקון מסך-הבית באייפון לא יתנתק בכל יציאה** | ביצוע התוכנית שסוכמה בשורה למטה. הבעיה: באייפון, אפליקציה שנפתחת מאייקון מסך-הבית (standalone) רצה במחסן אחסון מבודד מ-Safari שאייפון מנקה בכל סגירה, אז ההתחברות אבדה בכל פעם (ב-Safari הרגיל נשמרה). **התיקון:** קובץ חדש `src/lib/cookieStorage.js` ששומר את ה-session בעוגייה עם תוקף שנה, ומפצל אותה לחלקים (chunking) כי ה-session גדול מ-4KB; `src/lib/supabase.js` מפנה את Supabase לאחסון הזה במקום localStorage; `src/lib/AuthContext.jsx` מבקש `navigator.storage.persist()` באתחול. **אומת בדסקטופ מול מסד הבדיקות** (לא רק במסך): ההתחברות נקראת מהעוגייה, שורדת רענון וטאב חדש (= פתיחה מחדש של האפליקציה), הטוקן מתחדש אוטומטית וכותב בחזרה לעוגייה, signOut מנקה, ואין דליפה ל-localStorage באף מסלול; הפיצול נבדק עד 9.5KB (11 חלקים, חזרה זהה) כולל ניקוי חלקים עודפים בהקטנה. **מחיר חד-פעמי:** המשתמש התנתק פעם אחת בפרודקשן (מעבר בין מחסני אחסון). גיבוי ודאי אם ייכשל באייפון: להחזיר את האייקון להיפתח כמו Safari. **ממתין לבדיקת המשתמש מהאייפון.** פירוט ב-memory `project_ios_pwa_session_persistence`. | פרוס (main `c94caa0`) |
| 2026-07-23 | 🐛🧭 | חשבון/PWA | **תיקון ה-PWA לא פתר את בעיית ההתחברות ב-standalone; סוכם מעבר לעוגייה בסשן נפרד** | המשתמש בדק אחרי הפריסה: מאייקון מסך-הבית (מצב אפליקציה) עדיין מתנתק בכל יציאה, ומ-Safari הרגיל נשמר. גם באייפון עדכני. אובחן (כולל מחקר ברשת): מגבלה ידועה של iOS, ל-standalone PWA מחסן אחסון מבודד מ-Safari שמתנקה. אפליקציות אחרות "זוכרות" כי הן נייטיב עם Keychain, שאתר לא ניגש אליו. **הוחלט (לסשן חדש):** לעבור לשמירת ה-session בעוגייה עמידה במקום localStorage + `navigator.storage.persist()`, לאמת בדסקטופ, ולפרוס פעם אחת לפרודקשן לבדיקת המשתמש (הוא יכול לבדוק רק בפרודקשן, כי סביבת הבדיקה חסומה בסיסמה). אין כרגע משתמשים אחרים, אז המעבר לא ישבור אף אחד. גיבוי ודאי אם ייכשל: להחזיר את האייקון להיפתח כמו Safari. פירוט מלא ב-memory `project_ios_pwa_session_persistence`. | פתוח (לסשן חדש) |
| 2026-07-23 | ✨🔒 | חשבון/PWA | **matkon הפך לאפליקציה שניתן להתקין (PWA), כדי שההתחברות תישמר בין כניסות** | המשתמש דיווח שהוא נכנס מאייקון על מסך הבית (גלישה רגילה, לא פרטית) ובכל זאת נאלץ להתחבר מחדש בכל פעם. אובחן בקוד: מנגנון ה-session תקין (Supabase שומר ב-localStorage + חידוש אוטומטי, אין יציאה יזומה), אבל ל-`index.html` היו רק `apple-touch-icon` בלי `manifest` ובלי תגיות `apple-mobile-web-app`. בלי אלה אייפון לא "מתקין" את האתר כאפליקציה עצמאית ומתייחס לאחסון ההתחברות כזמני-שמותר-למחוק. **התיקון:** נוסף `public/manifest.webmanifest` (`display:standalone`, רקע/נושא נייבי `#080d1c`, אייקונים 192/512), תגיות `apple-mobile-web-app-capable=yes` + status-bar + title=MATKON + `theme-color`, ו-`apple-touch-icon` ב-180px (נגזרו מ-favicon.png הקיים דרך sips). **אומת חי:** ה-manifest נטען ותקין (200, standalone), כל האייקונים 200, האתר עדיין תקין ואפס שגיאות קונסול. **⚠️ למשתמש:** צריך למחוק את האייקון הישן ממסך הבית ולהוסיף מחדש כדי שאייפון יתקין עם ההגדרות החדשות, ואז לבדוק שההתחברות שורדת. הסתייגות: התנהגות האחסון של אייפון לא מובטחת ב-100%, זה התיקון הסטנדרטי שמכוון לגורם הסביר ביותר. | פרוס |
| 2026-07-23 | ✨🧭 | UX/פרופיל | **עמוד הפרופיל הפך למסך תצוגה: פרטי הפרופיל מוצגים בעמוד עצמו (קריאה בלבד), במקום רשימות מתכונים** | עד היום עמוד הפרופיל הציג טאבים ורשימות ("המתכונים שלי" + "שמורים"), ורק לחיצה על "עריכת פרופיל" חשפה את הפרטים. **הוחלט (המשתמש):** רשימות המתכונים נגישות ממילא מעמוד "מתכונים", אז הוסרו לגמרי מהפרופיל; במקומן העמוד מציג עכשיו את פרטי הפרופיל במצב תצוגה בלבד: שם פרטי, שם משפחה (מחולקים כמו באיסוף בהרשמה), מדינת מגורים + דגל, משפט קצר (ביו), ו-Email. **סרגל הסטטיסטיקות נשאר** (מתכונים/משותפים/לייקים/שמורים). כפתור "עריכת פרופיל" ממשיך לפתוח את חלון העריכה הקיים בלי שינוי (פעולות חשבון/פרטיות רגישות נשארות שם בלבד, לא חשופות במסך הראשי). `Profile.jsx` נוקה: הוסרו הטאבים, `loadSaved`, ו-`RecipeCard`; `loadRecipes` מצומצם ל-id+is_public (רק לצורך הסטטיסטיקות). **אומת חי בפריסת בדיקה** (מחובר כמשתמש הבדיקה): תצוגה, פיצול השם, וכפתור העריכה. הוחלט לבינתיים להשאיר את העריכה כפופאפ (לא inline). | פרוס |
| 2026-07-23 | ✨ | UX/רשימת קניות | **בוטל הפופאפ בהוספת מצרכים לרשימת הקניות מעמוד המתכון** | לחיצה על "הוסיפו לרשימת הקניות" ב-`RecipePage` פתחה drawer שהציג שוב את כל המצרכים, בגובה לא נכון בלי גלילה, ותוך כדי הריץ תרגום שמות מצרכים, שלב מיותר בשלב הזה. עכשiו הלחיצה פשוט מוסיפה את המצרכים לרשימה (כמו קודם) ומציגה הודעת אישור קצרה ("נוסף לרשימת הקניות ✓"); התרגום מתחולל ממילא רק בכניסה לעמוד "קניות". הוסר כל ה-drawer + מצבי ה-state שלו. **אומת חי בפריסת בדיקה** (מחובר): אין פופאפ, המצרך מגיע לעמוד קניות עם תג המונה. | פרוס |
| 2026-07-23 | ✨ | תשתית/דומיין | **נרכש matkon.co.il, מחובר כהפניה קבועה (308) ל-www.matkon.co** | נרכש ב-INTERNIC (אינטרספייס) עם שרתי ה-DNS הרגילים של הספק (sitesdepot.com, ברירת מחדל, לא שרתים חיצוניים). חובר בפרויקט Vercel כ-**Redirect** ולא כ-Environment, כדי שלא ירוץ כאתר עצמאי אלא רק יפנה לכתובת הקאנונית הקיימת (308 Permanent, אותו סוג הפניה ש-matkon.co עצמו כבר משתמש בו אל www.matkon.co). ב-Zone editor של INTERNIC הוחלפה רשומת ה-A של הדומיין מברירת המחדל של דף חנייה (80.244.162.32) לכתובת ה-IP של Vercel (216.150.1.1); רשומת www כבר הייתה CNAME לשורש והתעדכנה אוטומטית. הנפקת תעודת ה-SSL נכשלה בניסיון הראשון (`Failed To Generate Cert`, http-01 challenge) בגלל הפצת DNS חלקית, ועברה בניסיון חוזר אחרי כחצי שעה. **אומת בדפדפן בפועל:** matkon.co.il נטען ומפנה ל-matkon.co, תעודת אבטחה תקינה. אגב כך אומת גם ש-matkonapp.com (נרכש קודם ב-GoDaddy) כבר מפנה בהצלחה ל-matkon.co. | בוצע (שינוי DNS+Vercel בלבד, לא קשור ל-git) |
| 2026-07-22 | ✨ | רשימת קניות | **"בצל קצוץ" ו"בצל" מתמזגים לשורה אחת ברשימת הקניות; "עגבניות מרוסקות" נשארת נבדלת מ"עגבניות"** | הכנה בקראת השקת המתנסים. `api/translate-ingredients.js` מחזיר עכשיו גם `name_shop`, שם קנייה עברי נקי שה-AI מפיק: מסיר תיאורי הכנה (קצוץ/פרוס/מגורר/כתוש) אך שומר על מילים שמזהות מוצר שונה לגמרי (משומר/רסק/טחון/אבקה). `src/lib/shopping.js` ממזג פריטים לפי `name_shop` אחרי התרגום (לא בזמן ההוספה, כי אין עדיין AI באותו רגע); פריטים מסומנים לעולם לא מתמזגים. **אומת חי מול AI אמיתי בפריסת בדיקה** (לא רק בקוד): "בצל קצוץ"→"בצל", "3 שיני שום כתושות"→"שום", ולעומת זאת "עגבניות מרוסקות" ו"אבקת שום" נשארו נבדלים מ"עגבניות" ו"שום" בהתאמה (בדיוק הסיכון שנשקל מראש: שימורים/אבקה הם מוצר שונה, לא הכנה). גם "חלב"/"חלבה" (רגרסיה ישנה) נבדקו ונשארו נפרדים. לוגיקת המיזוג נבדקה גם ביחידה (5 תרחישים, כולל פריט מסומן שלא נוגעים בו). | בענף `ux-redesign-preview`, אומת בפריסת בדיקה, טרם ב-main |
| 2026-07-22 | ⚖️ | משפטי/פרטיות | **מדיניות הפרטיות עודכנה: נוספה שורת Perplexity, וטבלת הספקים (סעיף 5) עברה מרשימת שמות לרשימת קטגוריות** | הכנה לקראת השקה. Perplexity (חיפוש חנויות למצרך נדיר, מוזג לפרודקשן ב-21.7) חסרה מהטבלה; Supadata כבר היה שם מ-20.7. מיקום Perplexity אומת ממקורות רשמיים (San Francisco, ארה"ב), לא ניחוש. **תוך כדי עלה שיקול נוסף מהמשתמש:** רשימת ספקים בשמות מלאים חושפת את מפת ה-AI של האפליקציה (אילו ספקים, מה בדיוק כל אחד עושה) — במיוחד Anthropic/Perplexity/Supadata שרצים רק בצד שרת ולא גלויים בשום מקום אחר. הוחלף לתיאור לפי **סוג ספק** (״ספקי בינה מלאכותית״ וכו') במקום שמות, שעומד בדרישת הגילוי של GDPR/NZ (סוגי נמענים, לא שמות ספציפיים חובה) ומצמצם את החשיפה. עודכן בשלושה מקומות ביחד: `legal/privacy_draft.md` (טיוטה, כולל מיפוי מלא שם-ספק↔קטגוריה להערות עו"ד), `legal/site/privacy_he.md` ו-`legal/site/privacy_en.md` (מה שמוצג בפועל באתר). תאריך "עדכון אחרון" עודכן ל-22.07.2026 בכל שלושת הקבצים. **אומת חי בפריסת בדיקה** (לא רק מקומי): הטבלה החדשה והתאריך מוצגים נכון בעמוד `/privacy`. **כלל חדש נשמר לזיכרון:** כל שינוי תוכן במסמך משפטי → לעדכן גם את תאריך "עדכון אחרון". | בענף `ux-redesign-preview`, אומת בפריסת בדיקה, טרם ב-main |
| 2026-07-21 | 🐛 | חיפוש מצרכים נדירים | **תוקן: קאש חיפוש ללא תוקף, ונוקה קאש הפרודקשן** | המשך ישיר לשורת המעבר ל-Perplexity למטה. נוסף תוקף של 30 יום לרשומת קאש; רשומה שפגה מטופלת כמו קריאה חדשה, וה-write path מוחק את הרשומה הישנה לפני כתיבת החדשה (יש אילוץ ייחודיות (ingredient, country) שהיה גורם לכתיבה החדשה להיכשל בשקט בלי המחיקה). אומת ישירות מול מסד הבדיקות: רשומה מזויפת בת 40 יום עם תשובה שגויה הוחלפה נכון בתשובה טרייה, ואז אותה רשומה הטרייה חזרה כ-cache hit מהיר בקריאה הבאה. בנוסף, קאש הפרודקשן (מהעידן הישן של אנתרופיק, רק 2 רשומות) **נוקה לגמרי** לפי בקשת המשתמש: אחת מהן הייתה הבאג המתועד למטה, "quality zaatar" תחת חיפוש שמקורו ב"סחוג". | פרוס (main `69f32b4`) |
| 2026-07-21 | ✨ | חיפוש מצרכים נדירים | **המהירות נפתרה: מעבר מאנתרופיק ל-Perplexity Sonar Pro** | הושוו בפועל, לא בתיאוריה, אותם 4 מצרכים (זעתר/טחינה גולמית/פתיתים/סחוג, ניו זילנד) מול Perplexity, אחרי שנוסה קודם צמצום סבב חיפוש אנתרופיק מ-2 ל-1 (לא פתר את המקרה הקשה, "סחוג" עדיין נתקע ב-60 שניות). Perplexity ענה תוך 9-14 שניות דרך כל הצנרת האמיתית (הרשאה+קאש+הגבלת קצב+תקציב+בדיקת קישור-חי), כולל "סחוג" שקודם נכשל תמיד. עלות לקריאה ירדה מכ-10 סנט לכ-0.9 סנט (נמדד, לא מוערך). התגלה בדיקה: Perplexity ממציא דומיינים לא-קיימים בדיוק כמו הבעיה הישנה אצל אנתרופיק (7 מתוך 12 קישורים לא עבדו בבדיקה ראשונה), אז נשמרה אותה בדיקת קישור-חי בשרת, והמודל מתבקש עכשיו על 5 מועמדים במקום 3 כדי שיישארו 2-3 אחרי הסינון. אומת ישירות מול מסד הבדיקות (לא רק דרך המסך): גם שנשמר בקאש נכון, וגם שרשומת קאש ישנה ושגויה מ"פתיתים" (מלפני תיקון אתמול) נמחקה. בנוסף נבדק ונדחה `search_context_size: high` (פרמטר אמיתי בתיעוד הרשמי): לא שיפר את שיעור הקישורים התקינים (35% מול 42% בברירת המחדל), עלה כמעט פי 2, וגרם לתרגום שגוי אחד ("פתיתים"→אורזו). | פרוס (main `94eb75b`) |
| 2026-07-21 | ✨ | רשימת קניות | **בוטלה ספריית המחוקים, מחיקה תהיה מיידית ולצמיתות** | ביצוע ההחלטה שנרשמה קודם היום (שורה למטה). הוסר כל מנגנון ה-`deleted` array מ-`lib/shopping.js` (`moveCheckedToDeleted`, `restoreDeletedItem`, `permanentlyDeleteItem`, `clearDeletedLibrary`, וגם `removeChecked`/`clearAll` שכבר לא היו בשימוש בכלל) לטובת פונקציה אחת, `deleteCheckedItems`. הוסר כפתור "ספריית מחוקים" והמגירה מ-`Shopping.jsx`, ומחלקת ה-CSS הלא-נחוצה. אומת חי בפריסת הבדיקה: התחברות כמשתמש בדיקה אמיתי, סימון פריטים ומחיקה, כולל בדיקה שמשתמש עם נתונים ישנים בדפדפן (מגרסה קודמת) לא רואה שום שובר. | פרוס (main `94eb75b`) |
| 2026-07-21 | 🐛 | רשימת קניות | **תוקנה גלילת מגירת "ספריית מחוקים" (90vh)** | אותה תבנית באג בדיוק כמו מגירת "הוספה לרשימה" שתוקנה קודם היום: לא היה מספיק גובה לגלול עד לכפתור "מחיקת הספרייה לצמיתות" (38 פריטים אצל המשתמש). תוקן זהה: 85vh→90vh. שינוי CSS בלבד, לא נבדק ישירות בדפדפן/מובייל (רק build+eslint), ולבסוף הוחלט לבטל את המגירה כליל (ראו שורה למעלה). | פרוס, יוחלף בביטול המגירה |
| 2026-07-21 | 🐛 | חיפוש מצרכים נדירים | **תוקן: קריאה שנתקעה עד timeout מלא של 300 שניות** | אחרי צמצום סבבי החיפוש (3→2), התגלה בלוגי הפרודקשן שקריאה על "סחוג" עדיין רצה עד המגבלה הקשה של השרת (Vercel, 300 שניות) ואז נכשלה, כלומר המשתמש חיכה 5 דקות לשום דבר. נוספה תקרת זמן מפורשת של 60 שניות: כשל מהיר וברור במקום המתנה ארוכה. אומת: אותה שאילתה חזרה תוך 52.8 שניות (הפעם בלי תוצאות, אבל בלי תקיעה). | פרוס |
| 2026-07-21 | 🧭 | רשימת קניות | **הוחלט: לבטל את "ספריית מחוקים", מחיקה תהיה מיידית ולצמיתות** | מגירת ספריית המחוקים לא נגללת כראוי גם אחרי תיקון גובה (ראו שורת "תיקון גלילה" למטה), ועלה חשד לא-מאומת שפריט יכול להימצא גם ברשימה הפעילה וגם במחוקים בו-זמנית. המשתמש קבע שהתכונה לא קריטית, "אם ידרש בעתיד נחזיר אותו". פירוט ב-`memory/project_shopping_remove_deleted_library.md`. | בוצע, ראו שורה למעלה |
| 2026-07-21 | 🧭 | חיפוש מצרכים נדירים | **המהירות עדיין לא מספיקה, נקבע כמשימה ראשית לסשן הבא** | אחרי כל תיקוני האיכות והמהירות של הערב (25-53 שניות, תקרת 60 שניות), המשתמש בדק ידנית את אותה שאילתה בדיוק ("איפה למצוא סחוג באוקלנד") ב-ChatGPT וקיבל תשובה מעולה תוך כ-1.5 שניות, כולל 4 חנויות ספציפיות והבחנה בין סחוג גנרי למותגים ישראליים. הוגדר כליבת הערך של האפליקציה לישראלים בחו"ל. פירוט ב-`memory/project_rare_ingredient_search_quality.md`. | פתוח (הבא בתור) |
| 2026-07-21 | 🐛 | רשימת קניות | **תרגום מחדש בכל הוספה, חיפוש מצרך נדיר מהיר יותר, צבעים עקביים** | בבדיקת E2E: פריט שנמחק וחזר לרשימה שמר את התרגום השגוי הישן, כי `addIngredientsToList` (`src/lib/shopping.js`) העתיק את הערך הקיים בלי לנקות אותו. תוקן: כל הוספה/שחזור מפריט מחוק מוחקת את `name_local` הישן ומחשבת קטגוריה מחדש, כך שהתרגום המעודכן תמיד יורץ בטעינה הבאה. בנוסף: חיפוש מצרך נדיר (`api/find-rare-ingredient.js`) הצטמצם משלושה סבבי חיפוש לשניים, כי שלושה סבבים לקחו 3-4 דקות, מספיק זמן שרשת סלולרית מנתקת את החיבור לפני שהתשובה מגיעה (אומת בלוגים: השרת כן הצליח, המשתמש קיבל שגיאה). נבדק אחרי: 25 שניות במקום 3-4 דקות, עדיין 3 חנויות תקינות. גם תוקן חוסר עקביות: מגירת "הוספה לרשימה" בתוך המתכון הציגה את התרגום באפור, בעוד עמוד הקניות מציג אותו בצהוב מודגש, אוחד לצהוב בכל מקום. | פרוס |
| 2026-07-21 | 🐛 | תרגום מצרכים (רשימת קניות) | **עשבי תיבול טריים סווגו כתבלינים, שדרוג מודל לתרגום מונחי מטבח נישתיים** | בבדיקת E2E: "זעתר" תורגם ל-"thyme" (תימין, צמח אחר) וסווג תחת "ירקות", ו"סחוג" תורגם בטעות ל-"quality zaatar". שני תיקונים: (1) הכלל בהנחיה וברשימת המילים הקבועה (`shopping.js`) שינה מ"עשבי תיבול טריים → ירקות" ל"עשבי תיבול (טריים או יבשים) → תבלינים", לבקשת המשתמש. (2) חידוד ניסוח כללי ("אל תחליף מצרך במצרך אחר שדומה לו בהקשר") לא פתר את הבלבול בין זעתר לסחוג, כי זו הייתה מגבלת ידע של המודל (Haiku) ולא בעיית ניסוח. שודרג ל-Sonnet 5, אותו תיקון שכבר עבד בחיפוש מצרך נדיר. תקלה נלווית: Sonnet 5 מפעיל כברירת מחדל שלב "חשיבה" לפני התשובה, מה שגרם לקוד (שהניח שהתשובה הראשונה היא תמיד הטקסט) לקרוס; תוקן. | פרוס |
| 2026-07-21 | 🐛 | פירוק מתכון מלינק | **תוקנו שתי תקלות שהסתירו זו את זו בייבוא מתכון מהארץ** | בבדיקת E2E: ניסיון לייבא מתכון מ-haaretz.co.il נכשל בהודעת "לא ניתן לגשת ללינק". אומת ישירות מול האתר: השרת נחסם על ידי זיהוי-בוט (403), דפדפן רגיל עבר (200). תוקן שינוי הזיהוי לדפדפן רגיל. זה חשף תקלה שנייה שהייתה מוסתרת: אחרי שהגישה לדף עבדה, תשובת ה-AI שמפרק את המתכון נחתכה באמצע (מגבלת אורך תשובה נמוכה מדי למתכון עם רשימת מרכיבים ארוכה ומספר רכיבים כמו בצק/מילוי/הגשה), מה שגרם לשגיאת פענוח JSON. הועלתה תקרת האורך. | פרוס |
| 2026-07-21 | 🐛 | חיפוש מצרכים נדירים | **תוכנית התיקון מ-20.7 מומשה: אימות קישורים בשרת, שדרוג מודל, חידוד הנחיה** | ראו אבחון מפורט בשורת 2026-07-20 למטה. שלושת חלקי התוכנית בוצעו: (1) כל קישור שה-AI מחזיר נבדק עכשיו בפועל מול האתר (בקשת רשת אמיתית) לפני שהוא חוזר למשתמש או נכנס לקאש המשותף — קישור שלא עונה בהצלחה נופל מהרשימה. (2) המודל שודרג מ-Haiku ל-Sonnet 5. (3) ההנחיה חודדה לזהות את המצרך בהקשר קולינרי לפני תרגום, ולא לתרגם מילולית. אומת מול אותם 3 מצרכים מהאבחון (זעתר/טחינה גולמית/פתיתים): "פתיתים" כבר לא מחזיר פירורי לחם אלא "Israeli couscous", וכל הקישורים שהוחזרו נבדקו ונמצאו חיים. | פרוס |
| 2026-07-20 | 🐛 | חיפוש מצרכים נדירים | **אובחנו שתי תקלות איכות בנתוני אמת. תוכנית תיקון אושרה, טרם מומשה** | המשתמש סימן "עובד, לא מספיק טוב". במקום לנחש, הורצה הפונקציה הקיימת (`api/find-rare-ingredient.js`, Haiku + web_search) מול פריסת הבדיקה עם 3 מצרכים אמיתיים ומדינה=ניו זילנד. **(1) מוצר שגוי מתרגום מילולי:** חיפוש "פתיתים" החזיר 3 קישורים לפירורי לחם פנקו, כלומר תורגם flakes→breadcrumbs במקום פסטה ישראלית. זעתר וטחינה חזרו סבירים, אז הכשל תלוי-מצרך. **(2) קישורים מתים, 2 מתוך 9 (22%):** נבדקו ב-curl עם דפדפן אמיתי, אחד 404 (דומיין קיים, נתיב-מוצר מומצא) ואחד דומיין שלא נפתר כלל (חנות מומצאת). ההנחיה כבר אומרת מפורשות לא להמציא כתובות, והמודל ממציא בכל זאת, ולכן **הנחיה לבדה לא תפתור**. תוכנית שאושרה: (א) לאמת כל קישור בשרת לפני החזרה ולפני הכניסה לקאש, זה החלק הוודאי; (ב) שדרוג מודל; (ג) חידוד הנחיה לזהות את המצרך במטבח לפני החיפוש. פירוט ב-`memory/project_rare_ingredient_search_quality.md`. | פרוס (התוכנית מומשה 21.7, ראו שורה למטה) |
| 2026-07-20 | 🐛 | רשימת קניות + משפטי | **ארבעה תיקונים: גלילת חלון המצרכים, איחוד מצרכים זהים, קישור למדיניות הפרטיות, ותאריכים** | **(1) חלון "הוספה לרשימת המצרכים"** נפתח נמוך מדי ואי אפשר היה לגלול עד סופו. הועלה ל-90% מגובה המסך, בדיוק כמו שחלון עריכת הפרופיל כבר עושה. ⚠️ **ניסיון ראשון נפסל ע"י המשתמש בצדק:** תיקנתי בכלל ה-CSS המשותף כך שהחלון יצויר מעל הסרגל התחתון, מה שהסתיר את הסרגל וסתר כלל קיים באפליקציה (הסרגל תמיד גלוי). הוחזר, והתיקון בוצע מקומית לחלון הזה בלבד. **(2) איחוד מצרכים:** "ביצים גודל L" ו"ביצים" נשארו שתי שורות; `normalizeIngredientName` מסיר עכשיו תיאורי גודל לפני ההשוואה. באג שנתפס תוך כדי: `\b` ב-JS לא עובד סביב אותיות עבריות (ASCII בלבד) והכלל לא תפס עד שהוסר. **(3) פער משפטי אמיתי שהמשתמש איתר:** מסכי ההרשמה (רגילה ו-Google) ביקשו לאשר את מדיניות הפרטיות אבל קישרו רק לתנאי השימוש, כך שהמסמך שמאשרים לא היה נגיש מהמסך שדורש את האישור. שניהם מקשרים עכשיו לשני המסמכים. **(4) תאריך 20.07.2026** במקום placeholder, ושורת Supadata, **בכל ארבע השכבות**: טיוטות עבודה, קבצי `legal/site/` שהאפליקציה מרנדרת בפועל, חבילת עו"ד ב-md, ושני קובצי ה-Word (נערכו ב-XML הפנימי כדי לשמר עיצוב). 🔎 **תגלית שחשפה פער ותיק:** עמוד הפרטיות קורא מ-`legal/site/`, לא מהטיוטה, וזה לא היה מתועד; לכן עדכון §5 בטיוטה בלבד לא היה מגיע לאתר. תועד ככלל בטיוטה. כל התיקונים אומתו חי מול מסד הבדיקות. | פרוס בענף (`b40bb35`), טרם ב-main |
| 2026-07-20 | ✨ | תוכן/הוספת מתכון | **הוספת מתכון מלינק טיקטוק/אינסטגרם/יוטיוב עובדת, דרך Supadata `/extract`** | ממשיך את הבאג שנרשם למטה (2026-07-20, "התגלה"). לפני כתיבת קוד: 3 בדיקות איכות ישירות מול Supadata (2 טיקטוק + 1 אינסטגרם) החזירו מתכון מלא ומדויק (מרכיבים+כמויות+שלבים). מימוש: `api/_video-extract.js` חדש (זיהוי לינק-וידאו, שליחת בקשה + פולינג עד 50 שניות, המרה לטקסט עברי); `api/parse-recipe.js` מזין את הטקסט הזה לאותו LLM קיים שכבר מתרגם/מקטלג/מתייג, במקום לבנות צנרת נפרדת. **תקרת שימוש חודשית משלנו** (לא מסתמכים על מסך החיוב של הספק): מיגרציה חדשה (`video_extract_budget_status`, מראה של תקרת ה-AI הקיימת) חוסמת אחרי 15 קריאות בחודש, מתחת לשכבה החינמית. מודרציה ולינק-מקור לא דרשו שום שינוי קוד, שניהם כבר מכוסים בצנרת הפרסום הקיימת בלי קשר למקור התוכן. **אומת פעמיים לפני עלייה לפרודקשן:** תחילה מקצה-לקצה בסביבת הבדיקות (לינק טיקטוק אמיתי דרך משתמש בדיקה מחובר על פריסת Preview, ואומת גם ישירות מול usage_log במסד), ורק אז הוחל אותו תהליך על הפרודקשן (מפתח כרשומת-Vercel נפרדת, מיגרציה עם בדיקת-גרסה, ואז push). דחוי בנפרד: תצוגת עלות בדשבורד הניהול (`matkon-admin`, פרויקט לא-מקומי) ושורת ספק במדיניות הפרטיות, שניהם רק אחרי שהפיצ'ר כבר חי. | פרוס (main, commit `647f4de`) |
| 2026-07-20 | 🐛 | מתכונים/עריכה | **תוקנה אי-התאמה בין ברירת המחדל של "מנות" בין המסכים (2 מול 4), שגרמה לשמירה שקטה לשנות את הכמות** | התגלה תוך כדי אימות חי של תיקון ה-lint: לחיצה על "שמרו שינויים" ב-EditRecipe בלי לגעת בשדה "מנות" שינתה בפועל את הערך השמור מ-4 ל-2. הסיבה: `RecipePage.jsx` הציג ברירת מחדל 4 כשאין ערך, אבל `EditRecipe.jsx` (state ראשוני + טעינה + שמירה) ו-`api/_moderation.js` (`sanitizeRecipe`, שרץ גם בפרסום וגם בעריכה) נפלו חזרה ל-2. אומת ישירות מול מסד הבדיקות: השדה נשמר על 4 אחרי שמירה ריקה. בוצע בשני שלבים — סשן מקביל (worktree נפרד) תיקן את שלושת המקומות העיקריים, והסשן הזה מצא ותיקן מקום רביעי שנשאר (`EditRecipe.jsx` בפונקציית השמירה עצמה) לפני המיזוג. | פרוס (main) |
| 2026-07-20 | 🧹 | תשתית/איכות קוד | **חוב ה-lint נסגר במלואו: 38 → 0 (react-hooks), נבדק חי בכל קובץ** | השלמה ל-38 שנותרו בשורה 18 למטה. כל 38 הבעיות היו `react-hooks` (תלויות אפקט, immutability, purity, set-state-in-effect) שנוגעות ישירות בהתנהגות בדפדפן, ולכן כל קובץ תוקן ואומת חי בנפרד (לא רק build ירוק) לפני commit: Recipes.jsx (10), CookingMode.jsx (6, כולל בדיקת טיימר אמיתי עם ריענון-דף מלא), Profile.jsx (6), RecipePage.jsx (5, כולל אימות ישיר מול המסד של מסלול הגולש האנונימי — אותה בדיקה שהייתה תופסת את תקלת ה-18.7), App.jsx, EditRecipe.jsx, ועוד 7 קבצים עם בעיה אחת כל אחד. **דפוסים חוזרים:** שגיאות "accessed before declared" תוקנו בהזזת פונקציות מעל ה-effect שקורא להן (ללא שינוי לוגי); קריאות `setState` תקינות בתוך effect תועדו עם `eslint-disable` והסבר במקום שינוי מבני, לפי התקדים הקיים (בלוקי catch ריקים); תלות אחת (`items` ב-Shopping.jsx) **הושארה בכוונה בחוץ** כי הוספתה הייתה יוצרת סיכון אמיתי ללולאת בקשות רשת אינסופית. תקרת ה-lint ב-`CLAUDE.md` עודכנה ל-0. | פרוס (main) |
| 2026-07-20 | 🐛⏳ | תוכן/הוספת מתכון | **התגלה: הוספת מתכון מלינק טיקטוק/אינסטגרם/יוטיוב לא עובדת. פתרון מוצע: Supadata** | באג שלא היה ברשימה. `api/parse-recipe.js` מושך HTML וגורד תגיות, מה שעובד לבלוגים אבל לא לפלטפורמות וידאו: הן בנויות ב-JavaScript (בקשה רגילה לא רואה תוכן), והמתכון עצמו נמצא **בקול או בטקסט שעל גבי הווידאו**, לא ב-HTML. המשתמש בדק במקביל עם ChatGPT ומצא את **Supadata**. נבדק לעומק: לשירות שני אנדפוינטים וההבדל קריטי. `/transcript` מתמלל **קול בלבד** (לא יתפוס סרטון שקט עם מרכיבים ככיתוב על המסך), ואילו **`/extract` מנתח גם קול וגם טקסט חזותי** ומחזיר JSON לפי schema, **עם דוגמה מתועדת אצלם בדיוק למתכוני בישול**. תומך ב-YouTube/TikTok/Instagram/X/Facebook. מחיר: 5 קרדיטים לדקת וידאו (100 קרדיטים חינם בחודש, כ-20 מתכונים; בתשלום מ-17$/חודש). **למה שירות מנוהל ולא בנייה עצמית:** MATKON רץ על Vercel (פונקציות זמניות, אין שרת קבוע), ותחזוקת גרידה מול פלטפורמות שמשנות מבנה תדיר היא נטל מתמשך. לא נמצאה חלופה שקולה. **לפני מימוש: לבדוק 2-3 לינקים אמיתיים** ולוודא איכות בפועל. תיעוד מלא ב-`memory/project_video_recipe_extraction.md`. | ⏳ בוצע, ראו שורה למעלה (2026-07-20, ✨) |
| 2026-07-20 | 🧹 | תשתית/איכות קוד | **חוב ה-lint צומצם: 73 בעיות → 38 (3 commits, נפרס)** | סבב ניקוי לבקשת המשתמש, בשלושה שלבים לפי רמת סיכון. **(1) פער בהגדרת הכלי (`4cd125d`):** `eslint.config.js` החיל הגדרות דפדפן על **כל** הקבצים, כולל `api/` שרץ כ-Node, ולכן כל `process.env` בקוד שרת סומן כשגיאה שקרית. הוגדר `api/**/*.js` עם globals של Node. **73→66 בלי לגעת בהתנהגות.** **(2) ממצאים מכניים (`ea0b262`):** כל ממצא נבדק בהקשרו לפני נגיעה. `COUNTRY_FLAGS` הכיל 'גרמניה' פעמיים **עם אותו דגל** (כפילות מיותרת, לא באג ולא ערך שנדרס); רגקס תווי-בקרה ב-2 קבצים הוא **ניקוי-קלט מכוון** ולכן תועד ולא שונה; הוסרו 8 ייבואים/משתנים מתים ב-6 קבצים. **66→53.** **(3) בלוקים ריקים + Fast Refresh (`0d63950`):** 13 בלוקי `catch {}` נבדקו והם כולם דפוסי best-effort אמיתיים (כתיבה ל-localStorage, חלון שיתוף מובנה, ניקוי במחיקה), ולכן קיבלו **הערת הסבר** במקום שינוי התנהגות. בנוסף `AuthContext.jsx` ייצא גם קומפוננטה וגם hook, מה ששובר את Fast Refresh של Vite, ולכן **פוצל ל-3 קבצים** (`authContextInstance.js` / `AuthContext.jsx` / `useAuth.js`) + עודכן הייבוא ב-11 קבצים. **53→38.** אומת בדפדפן: האפליקציה עולה, אפס שגיאות קונסול, ניווט והפניית-אימות עובדים. תקרת ה-lint ב-`CLAUDE.md` עודכנה ל-38. **נותר (לא בוצע):** 38 הבעיות הנותרות כמעט כולן `react-hooks` (תלויות אפקט, immutability, setState-in-effect), שנוגעות ישירות בהתנהגות בדפדפן. **סווג ככבד:** דורש בדיקה ידנית של כל מסך, לא רק build ירוק. | פרוס (main) |
| 2026-07-20 | 🔒🐛 | תשתית/סביבת בדיקה | **שני כשלים בסביבת הבדיקה שהתגלו רק כשנבדקה פעולת כתיבה אמיתית** | שניהם **חסמו את אימות משימת התמונות**, ושניהם לא נתפסו קודם כי כל האימותים על הפריסות עד היום היו **קריאה בלבד**, וקריאת מתכון ציבורי מצליחה גם בהרשאות אנונימיות. **(1) מפתח השרת ב-Preview היה שגוי:** `SUPABASE_SERVICE_ROLE_KEY` בסביבת Preview התנהג כאנונימי, ולכן `insert` לטבלת המתכונים נכשל ב-`42501 permission denied`. אומת מול המסד שלתפקיד השרת **יש** הרשאה ולאנונימי **אין**, כלומר הבעיה בערך שהוזן ולא במסד. המשתמש הזין את הערך הנכון (מסומן Sensitive) + פריסה מחדש. **כלל שנגזר: בדיקת Preview חייבת לכלול פעולת כתיבה, לא רק קריאה.** **(2) במסד הבדיקות לא היה אף מאגר אחסון:** שכפול ענף ב-Supabase מעתיק את מסד הנתונים אבל **לא את ה-Storage**, ולכן כל העלאת תמונה נכשלה ב-"Bucket not found". התברר ש-`recipe-images` בפרודקשן נוצר ידנית בעבר ומעולם לא היה במיגרציה (אותו דפוס סחף של הסכימה). **תוקן כמיגרציה** (`20260720014453_recipe_images_bucket`), idempotent, והוחלה על שני המסדים (בפרודקשן היא no-op בטוח). | פרוס |
| 2026-07-20 | ✨🔒 | תמונות/שיתוף | **כל תמונה נשמרת אצלנו, וגרסת שיתוף מוכנה מראש. באג התצוגה בוואטסאפ נסגר** | המשימה שנפתחה ב-18.7, בוצעה במלואה ונפרסה (`63f60a7`,`1c09447`,`f01a3d1`). **המנגנון:** `waitUntil` של Vercel (אומת מול התיעוד הרשמי כשיטה המומלצת לעבודת רקע ב-Node), כך שהפרסום **מסתיים מיד** והמשיכה מהספק קורית אחריו בשרת. זה היה המכשול המרכזי: משיכה סינכרונית נמדדה 2 עד 45 שניות והייתה מחליפה באג אחד באחר גרוע יותר. **מה נבנה:** `api/_image-pipeline.js` חדש (שמירה + מחיקה), עמודות `image_source` ו-`share_image_url`, גרסת שיתוף **קבועה 1200x630** שנבנית מראש במקום להיווצר בזמן שליחה, ומחיקת התמונה הישנה בהחלפה **רק אחרי** שהחדשה נשמרה. **החותמת "נוצר ב-AI" הועברה לקריאת העמודה** במקום זיהוי לפי כתובת הספק, אחרת היא הייתה נעלמת בשקט ברגע שהתמונה עוברת אלינו. **אומת חי בסביבת הבדיקה, שרשרת מלאה:** פרסום ללא תמונה (מסלול AI) חוזר תוך כ-5 שניות והתמונה מגיעה אלינו ברקע; פרסום עם תמונת משתמש **עובר בדיקת תוכן חזותית כרגיל** (7 שניות מול 5, ההפרש הוא קריאת הבדיקה עצמה); מתכון-זבל **נדחה כרגיל**; החלפת תמונה מוחקת את הישנה בפועל; מחיקת מתכון מוחקת את שתי התמונות. **מדד ההצלחה אומת ישירות:** קרולר וואטסאפ על מתכון חדש לגמרי מקבל כרטיס מלא עם תמונה שמצביעה אלינו, וגולש רגיל ממשיך לקבל את האפליקציה. **🧭 החלטת משתמש: 8 התמונות הישנות בפרודקשן נשארות כמו שהן** ("הם בסדר"), וימשיכו לעבוד דרך הנפילה-אחורה. אין מעבר חד-פעמי. | פרוס (main + DB) |
| 2026-07-20 | 🧭 | תשתית/מיגרציות | **כלל חדש: שם קובץ מיגרציה עוקב אחרי מספר הגרסה שנרשם בפרודקשן** | התגלה תוך כדי הפריסה: אותו קובץ מיגרציה, כשמוחל על מסד הבדיקות ואז על הפרודקשן, **מקבל מספר גרסה שונה בכל מסד** (הכלי מייצר חותמת זמן לפי זמן ההרצה). זו בדיוק המכניקה שיצרה את סחף המיגרציות שתועד ב-18.7. **הכלל:** הפרודקשן הוא מקור האמת לסכימה, ולכן שם הקובץ עוקב אחריו, **גם במחיר שינוי שם פעמיים באותו סשן** (פעם לפי הבדיקות, ואז שוב לפי הפרודקשן). בוצע בפועל ב-`f01a3d1`. נשמר ב-`memory/feedback_migration_filename_tracks_production.md`. | בוצע |
| 2026-07-18 | 🧭🔒 | תשתית/סביבת בדיקה | **הוקמה סביבת בדיקה, והתגלה שהיסטוריית המיגרציות לא יכלה לבנות מסד מאפס** | **הכשל:** `20260611015250_add_foreign_keys_for_joins` הפנתה ל-`public.recipe_comments` שאף מיגרציה מסודרת לא יצרה. לכן **כל** בנייה מאפס מתה בשלב 4 מתוך 40, וכל ענפי התצוגה המקדימה ב-Supabase דיווחו `MIGRATIONS_FAILED` **בשקט, במשך שבועות, כולל main**. **התיקון:** `20260603000000_baseline_schema.sql` נוצר **מקריאת סכימת הפרודקשן החיה** אובייקט-אובייקט, ואומת מולה בשש טביעות אצבע (עמודות, אילוצים, אינדקסים, מדיניות, הרשאות, פונקציות), idempotent. 29 הקבצים הישנים הועברו ל-`migrations_archive/` עם README שמסביר למה אין להריץ אותם. **הסיבה השורשית תועדה:** שום מנגנון אוטומטי לא קרא את `supabase/migrations/`, השמות בריפו ובמסד תאמו אבל **אף מספר גרסה לא**, כי כל שינוי הוחל ידנית דרך ממשק הניהול. התיקייה **נראתה** כמו מנגנון בלי להיות מחוברת לכלום. `CLAUDE.md` נכתב עם נוהל 5 הצעדים, בדיקת המסלול האנונימי, וכלל הברזל שכל מדיניות שנשענת על `auth.uid()` או קוראת טבלה אחרת חייבת לציין `to authenticated` במפורש. **אומת שהסביבה עושה את עבודתה:** הוחזר בכוונה באג ה-RLS מ-18.7 והוא **שוחזר** (42501), והחזרת המדיניות תיקנה. | פרוס |
| 2026-07-18 | 🔒🐛 | הרשאות/שיתוף | **תוקנה רגרסיית הרשאות חיה ששברה קריאת מתכונים לכל גולש לא מחובר** | הבאג הוכנס באותו בוקר: מדיניות "קריאת מתכונים שמורים" הוחלה על **כל** התפקידים, ולכן קריאה אנונימית של `recipes` הריצה תת-שאילתה על `saved`, שאין לאנונימי הרשאה עליה, וכל קריאת מתכון של גולש מנותק נכשלה ב-`42501`. **הוסווה בבדיקות** כי `RecipePage` נופל אחורה לנקודת הקצה שרצה בהרשאות שרת ועוקפת את מנגנון ההרשאות. זהו האירוע שהוליד את הכלל המרכזי ב-`CLAUDE.md`: **מסלול עוקף מסווה שבירה של הרשאות**, ולכן אימות נעשה ישירות מול המסד ולא דרך המסך. בנוסף באותו commit: **חזרה למתכון אחרי הרשמה** (`lib/returnTo`), כי אורח שפתח לינק משותף ונרשם נחת בפיד ואיבד את המתכון שבשבילו הגיע. נשמר ב-localStorage (המסע יוצא מהאפליקציה למייל אישור/SSO), קריאה-חד-פעמית, תפוגה 24 שעות, **ורק נתיבים פנימיים** כדי שלא יהפוך להפניה פתוחה. 10 בדיקות יחידה. | פרוס |
| 2026-07-18 | ✨ | שיתוף | **תצוגה מקדימה של מתכון בוואטסאפ (E10)** | הדבקת לינק בוואטסאפ הציגה כתובת חשופה, כי קרולרים לא מריצים JavaScript ולכן קיבלו מעטפת ריקה. נבנו `api/share-preview.js` (HTML לקרולרים בלבד, עם תגיות og; שם המתכון הוא טקסט משתמש ולכן מוברח), `api/og-image.js` (הקטנה ל-1200x630, כי וואטסאפ מפילה בשקט תצוגות עם תמונה כבדה מדי: 1.36MB לעומת כ-90KB), ו-`vercel.json` שמנתב `/recipe/:id` לתצוגה **רק** לפי סוג הדפדפן, כך שגולש אנושי מקבל את האפליקציה כרגיל. שתי נקודות הקצה מקבלות UUID מדויק ולעולם לא מציגות רשימה, לפי אותו מודל של `api/recipe.js`: **החזקה בלינק היא ההרשאה**. `sharp` ננעל ל-0.34.5 (שני עותקים של libvips בתהליך אחד גורמים לקריסות). **בנוסף (E10 חלקים 1-2):** לינק למתכון פרטי נפתח למי שמחזיק בו, בהפרדה מכוונת בין שיתוף אישי לבין נראות בקהילה. | פרוס |
| 2026-07-18 | ✨🧭 | תמונות/שקיפות | **חותמת "נוצר ב-AI" על תמונות מחוללות (K8)** | תמונת מתכון מחוללת כשהמבשל לא סיפק אחת, ואמירה מפורשת על כך היא **גם יושר כלפי הקורא וגם עמידה בכללי הסימון המתהדקים** סביב מדיה סינתטית. עוצב במשקל סימן-מים במכוון: בלי גלולה ובלי מסגרת. גרסה ראשונה עם רקע מלא נבנתה **ונדחתה** כי נקראה כתווית ומשכה את העין מהמנה. **ליטוש אחרי בדיקה חיה** על תצלום כהה ותצלום בהיר במיוחד (החשש היה טקסט לבן שנעלם על אוכל בהיר; נמדד שלא, כי שכבת ההצללה אטומה כ-90% בגובה שבו הסימן יושב, והדבר תועד ב-CSS כי כל שינוי בשכבה ישנה זאת בשקט): טקסט לאותיות רגילות במקום רישיות, 10.95px→9.44px, ועוצמה 50%→20%. המשתמש בחר 20% מהשוואה בו-זמנית של שלוש רמות, **אחרי שנאמר לו במפורש שסימן דהוי מדי מפסיק לתפקד כגילוי נאות.** הרמה תועדה ב-CSS כהחלטה ולא כפליטה. | פרוס |
| 2026-07-18 | 🐛 | הרשמה/רשימת קניות | **שלושה תיקונים: הבהוב שדה ההזמנה, קטגוריית ביצים, ואיחוד שמות מצרכים** | **(1)** מסך ההרשמה הבהב את שדה קוד ההזמנה: ברירת המחדל היא "שער סגור" (הגנה מכוונת: קריאה כושלת לא תפתח הרשמה), אבל היא **הוצגה כעובדה** לחצי שנייה ואז התהפכה. נוסף `gateResolved` שמחזיק את הטופס עד שהתשובה מגיעה; ההגנה עצמה לא שונתה. **(2)** ביצים היו מסווגות תחת בשר/דגים. הן פרווה, ולכן הסיווג שגוי באפליקציה עברית מודעת-כשרות, ופריט בודד לא מצדיק כותרת משלו, אז הועברו ל"אחר". **תוקן בשני המקומות** שמסווגים (טבלת הלקוח וכלל ה-AI ב-`translate-ingredients`), כי תיקון בלקוח בלבד היה נדרס בהעשרה הבאה. **(3)** פריטי קניות אוחדו לפי השוואת מחרוזות מדויקת, ולכן עגבניות/עגבניה/עגבנייה ישבו כשלוש שורות. נוסף `normalizeIngredientName` שמקפל ניקוד, גרשיים, כתיב מלא וסופיות. **שני באגים שהבדיקות תפסו לפני שנפרס:** קיפול אותיות סופיות לפני הסרת סיומות שבר כל רבים זכר (בצלים), והכלל הגנרי **איחד חלב עם חלבה**, שני מוצרים שונים לגמרי. סדר הפעולות הוחלף ונוספה רשימת חריגים. 21 מקרי בדיקה. | פרוס |
| 2026-07-18 | ⏳🧭 | תמונות | **התגלה: אף תמונת מתכון אינה שמורה אצלנו (5 מהספק החינמי + 3 מ-Unsplash)** | התגלה תוך כדי אבחון באג הוואטסאפ, וגדול ממנו. `publish-recipe` רק **בונה מחרוזת כתובת** ולא מוריד דבר: התמונה נוצרת אצל הספק בצפייה הראשונה ונמשכת ממנו לנצח. המשמעות: התמונות **יכולות להיעלם** אם הספק נופל, ו**יכולות להשתנות מעצמן** אם הוא מחליף מודל. **באג הוואטסאפ נבלע במשימה הזו ואינו עומד בפני עצמו:** ברגע שתמונת השיתוף שלנו ומוכנה מראש, הקרולר מושך קובץ סטטי במקום להמתין לפונקציה שממתינה לצד שלישי (נמדד 2.03 שניות בהתחלה קרה, ועד 44.9 שניות כשהספק עדיין מחולל). **נדחה במכוון** לסשן נפרד כי נוגע בפרסום, בעריכה ובמחיקה. **🧭 מסגור המשתמש, שנשמר:** זו משימת מוצר ולא היגיינת תשתית, כי השיתוף הוא ממהות האפליקציה, **ולכן מדד ההצלחה הוא מסלול השיתוף** ולא עצם העובדה שהקבצים אצלנו. בוצע ב-20.7. | הושלם (ראה 20.7) |
| 2026-07-15 | 🧹 | קוד/קומפוננטים | **Phase 1 — עורכי Add/Edit הושלמו (B2+B3+B4, +3 commits)** | המשך ה-pass. **B2 `StepEditor`** (שלבי הכנה, controlled steps+onChange), **B3 `TagInput`** (צ'יפים+הוספה+השלמה-אוטומטית מתגיות הקהילה; fetch כ-state פנימי), **B4 `VisibilityPicker`** (שיתוף-ירוק/פרטי-סגול). כולם משותפים בין AddRecipe שלב 3 ל-EditRecipe; helpers+states+imports כפולים נמחקו. אומת חי כל אחד (bypass זמני `?step=3`, הוסר). **✅ קבוצת עורכי Add/Edit (B1-B4) שלמה.** Commits `8513646`,`75d5841`,`1063cd0`. **נותר ב-Phase 1:** B5 Drawer, B6 Tabs, B7 EmptyState, B8 Toast, B9 UserIdentity (+יישור אנכי דגלים-לשם + `authorName` מ-RecipePage). | פתוח (branch) |
| 2026-07-15 | 🧹🧭 | קוד/קומפוננטים | **Phase 1 Componentization — C1+C2+C3+B1 הושלמו (5 commits על `ux-redesign-preview`)** | pass מכני לפי `ux_to_prod_plan.md` — איחוד markup משוכפל לקומפוננטים משותפים, אפס שינוי חזותי מול ה-PDF, אימות חי ב-375px אחרי כל קומפוננט. **🧭 החלטה נעולה: גיאומטריית הדר אחידה 16px** בכל האפליקציה (היו 3 גיאומטריות: 0/16/24; המשתמש בחר 16 אחרי השוואה חזותית משולשת). **C1:** `AppHeader` אומץ ב-Feed/Recipes/Shopping/Add/Edit (props חדשים `showBack`+`compact`). **C2:** אומץ ב-5 מסכי auth (prop `children` לתת-כותרת; CSS ‎`.auth-page:not(.legal-page) > .app-head`‎ שומר טופס ב-24px). **C3:** `RecipeCard` המשותף החליף כרטיסים מקומיים ב-Feed+Recipes; **תוקן סדר-דגלים בקומפוננט לקנוני** (ישראל מימין; היה הפוך, השפיע רק על Profile) + **🧭 החלטה: תמיד 2 דגלים** — משתמש ישראלי מקבל 🇮🇱🇮🇱 (פורמט אחיד, לא ייראה שונה). **B1:** `IngredientEditor` משותף (Add שלב 3 ↔ Edit), helpers כפולים נמחקו, אומת אינטראקטיבית. Commits: `af00d9f`,`dc80b74`,`4c6a9b5`,`04ab35e`,`fc4d44e`. **נותר ב-Phase 1:** B2 StepEditor, B3 TagInput, B4 VisibilityPicker, B5 Drawer, B6 Tabs, B7 EmptyState, B8 Toast, B9 UserIdentity (+יישור אנכי דגלים-לשם שביקש המשתמש + איחוד `authorName` האחרון מ-RecipePage). | פתוח (branch, טרם בפרודקשן) |
| 2026-07-15 | 🔒⏳ | אבטחה/מודרציה | **לבדוק: האם עריכת מתכון (EditRecipe) עוברת מודרציית תוכן מלאה** | הערת משתמש (15.7): לוודא שמשתמש לא יכול להחליף בעריכה תוכן תמים בתוכן אסור (למשל לכתוב במקום "ביצים" טקסט שמפר את כללי הקהילה). ידוע שהשרת אוכף `sanitizeRecipe` (הגבלות אורך) על עריכה — צריך לאמת שגם **שער המודרציה התוכני** (moderateRecipe) רץ על מסלול העדכון, לא רק על יצירה. אם לא — לסגור את הפער כמו שנסגר ל-bio/תגובות/שם. **✅ נבדק ונסגר (20.07): `api/update-recipe.js` שורה 43 מריץ את `moderateRecipe` המלא, לא רק `sanitizeRecipe`.** מסלול העריכה מוגן. | בוצע |
| 2026-07-13 | ⚖️🧭 | משפטי/תנאי שימוש | **תנאי שימוש — pass תוכן מלא (14 סעיפים), שיפוט הועבר לניו זילנד** | עלה בבדיקת מסך 17 (Terms). המשתמש ביקש לעבוד על **מהות התוכן**, לא רק עיצוב. נבנתה טיוטת עבודה `legal/terms_draft.md` עם טבלת סיכונים (severity×likelihood, סקיל legal-risk-assessment) ו-14 סעיפים שנוסחו ואושרו סעיף-סעיף: בטיחות מזון, זמינות (as-is), הגבלת אחריות (תקרה NZD 200), זכויות צרכן NZ (CGA/FTA), רישיון תוכן (§3 מאוזן, פתר סתירה עם §8), השעיה/חסימה (§7, תואם מסך Blocked, זכות ערעור), דין ושיפוט (§9 → NZ + הכרה בזכות צרכן מקומית), גיל מינימום (16), תוכן AI (כולל find-rare, בלי אחריות על חנות), זכויות יוצרים+נוהל הסרה (§2, copyright@), שיפוי, שינוי תנאים, סיום/מחיקת חשבון, חשבון והרשמה, בוילרפלייט; + תיקוני §1/§4. **החלטות מפתח:** מפעיל יחיד (אין ישות עדיין), שפה = עברית מחייבת + אנגלית לייחוס (תרגום בסוף), כל סעיף מסומן לליטוש עו"ד NZ לפני פרסום. **פתוח:** מדיניות פרטיות (מסמך נפרד, סשן ייעודי), תרגום EN, הרכבה ל-Terms.jsx. | טיוטה (branch; דורש עו"ד NZ) |
| 2026-07-13 | 🔒🐛 | אבטחה/חסימת משתמש | **פער אכיפת חסימה נסגר — משתמש חסום נחסם כעת מכל פעולת כתיבה (אכיפה בשכבת ה-DB)** | **הבעיה (התגלתה בבדיקת מסך 17 Blocked):** החסימה נאכפה רק ב-4 endpoints של service-role (publish/update/parse-recipe + moderate-image), אבל **כל כתיבה ישירה client→RLS** (תגובות, לייקים, שמירות, שיתופים, ואפילו insert/update ישיר של מתכון) **לא בדקה ban** — ה-policies בדקו רק בעלות (`auth.uid()=user_id`). כך משתמש שנחסם על תוכן פוגעני **עדיין יכול היה לפרסם תגובות פוגעניות**. ה-`BanGuard` בצד-לקוח הוא קוסמטי (עקיף בקלות עם token). **התיקון (מיגרציה `20260713172031_enforce_ban_on_all_writes` על הפרודקשן):** פונקציית `is_current_user_banned()` (`SECURITY DEFINER`, קוראת את `user_security` שהוא self-read-only) נוספה ל-`WITH CHECK`/`USING` של policies הכתיבה ב-`recipe_comments`/`likes`/`saved`/`recipe_shares`/`recipes` (insert+update). בנוסף: בדיקת ban נוספה ל-`find-rare-ingredient` ו-`translate-ingredients` (הגנת תקציב AI). **אומת חי:** ניסיון insert של לייק ע"י משתמש חסום (בטרנזקציה עם rollback) נדחה — `new row violates row-level security policy`; לא-חסום = no-op (התנאי `AND NOT false`). ללא רגרסיה לאפליקציה החיה. | פרוס (DB) · קוד ב-branch |
| 2026-07-13 | 🧭🧹 | UX/קהילה | **מסך 16 (Map + Community) — הוסר מהקוד לגמרי** | על branch `ux-redesign-preview`. במקום לעצב מחדש, המשתמש החליט למחוק — המסכים היו **יתומים** (אין אליהם כניסה מהאפליקציה; `/map` ו-`/community/:country` קישרו רק זה לזה) ורצו כולם על **mock data** (`mockCommunities`, מדינות+מספרים מומצאים), מנותקים מה-DB; כפתור "הצטרפות לקהילה" ייצג מודל שגוי (הצטרפות-בבחירה). **הקהילה האמיתית כבר קיימת** = טאב "קהילה" ב-Recipes (מתכוני `is_public` לפי מדינת-המגורים של היוצר; שמירה+תגובה על כל מתכון ציבורי דרך RecipePage, בלי הגבלת-מדינה). **נמחק:** `MapPage.jsx`, `Community.jsx`, imports+routes ב-`App.jsx`, export `mockCommunities` ב-`mock.js`, בלוק CSS (MAP+COMMUNITY) ב-`index.css`, ותלויות `leaflet`+`react-leaflet` (בשימוש רק במפה). אומת: אין שאריות, האפליקציה רצה נקי, `/map`→Splash. **עתידי:** תצוגת נתוני-קהילה/"מספרי כוח" תיבנה מהיסודות על נתוני אמת כשיוחלט על הכיוון. | פתוח (branch, טרם בפרודקשן) |
| 2026-07-13 | ✨🔒 | UX/פרופיל + חשבון | **מסך 15 Profile — עריכת פרופיל מאוחדת + שדות חשבון (Email/סיסמה) + קומפוננטים משותפים** | על branch `ux-redesign-preview` (**לא בפרודקשן**). **נקודות 9+10:** אוחד הכל ל-drawer יחיד "עריכת פרופיל"; הוסרו הכפתור+drawer הנפרד של "פרטיות ונתונים" וקומפוננט `EditModal` הכפול. סקשנים: (א) **פרטים אישיים** — שם פרטי+משפחה נפרדים (מפרק/מאחד `full_name`), מדינה=`CountrySelect`, ביו, "שמירת שינויים" ירוק-גלוס; (ב) **חשבון וכניסה** — **החלפת Email** (`updateUser({email})` → לינק אישור לכתובת החדשה, כמו ברישום; **לינק, לא קוד**, מוודא שהכתובת אמיתית) + **החלפת סיסמה** (נוכחית+חדשה, **re-auth** לפני `updateUser({password})`, [[feedback_highest_security]]) + לינק "שכחתם את הסיסמה?" (כרגע "בקרוב"); (ג) **פרטיות ונתונים** (מנעול סגול `#a78bff`) — passkeys, **ייצוא נתונים מושבת+"בקרוב"**, **מחיקת חשבון אדום-גלוס מעוצב**; (ד) **יציאה** outline שקט. **תוויות סטטיסטיקה → לבן** (חזרה מהחלטה קודמת של צבעי-פלטה). **⭐ קומפוננטים משותפים חדשים:** `NameFields`, `PasswordInput`, `passwordRules.js` (מקור-אמת) — **`Register.jsx` רופקטר** להשתמש בהם (מרונדר זהה). אומת חי, אפס שגיאות. פירוט ב-`ux_ui_v0.1.md`. | פתוח (branch, טרם בפרודקשן) |
| 2026-07-13 | ⏳ | תשתית/חשבון | **2 משימות תשתית נפתחו בסבב Profile (מעבר ל-UX)** | (1) **ייצוא-נתונים-למייל:** כרגע הכפתור מושבת עם "בקרוב"; דרוש ספק מייל טרנזקציוני (Resend, משתלב עם Vercel) + API key + הרחבת `api/account.js` (action export) שישלח את ה-JSON כקובץ מצורף למייל המשתמש → ואז להפעיל את הכפתור. **באפליקציה אין כרגע שום תשתית שליחת מייל** (רק Supabase auth) — הקמה מאפס. (2) **זרימת שחזור/איפוס סיסמה:** הלינק "שכחתם סיסמה?" ב-Login הוא stub מת, ובפרופיל נוסף כפתור מקביל שמציג "בקרוב" — דרוש דף `reset-password` (יעד redirect) + חיווט `resetPasswordForEmail` (Supabase שולח את המייל נייטיב, חסר רק הדף+החיווט). (3) **לאמת ב-Supabase שה-"Secure email change" דלוק** (אישור כפול). נשמר ב-`memory/project_account_infra_tasks.md`. | פתוח |
| 2026-07-09 | ✨ | UX/הוספת מתכון | **AddRecipe שלב 3 (עריכת מתכון) עוצב + אושר** | על branch `ux-redesign-preview`. **השלמת תגיות אוטומטית** משדה "הוסיפו תגית" — שולף תגיות קהילה קיימות מה-DB (`recipes.tags` של public לפי שכיחות) ומציע התאמות תוך הקלדה (אחידות תגיות, מניעת צמחוני/צמחונית/צמכונית; נבחרה אופציה "מ-DB" ולא רשימה סגורה). **אייקוני נראות צבעוניים** (כדור-ארץ שיתוף=ירוק, מנעול פרטי=סגול). **הנחיית טיימר** קלה בצהוב בראש השלב ("אם תזינו זמן בשלב הכנה ניצור טיימר אוטומטי... הימנעו מלחזור על אותו זמן בשלבים עוקבים"). **סינון שלבים/מצרכים ריקים בשמירה** (`saveRecipe`) — לא חוסם הוספת שלב ריק אבל לא שומר ריקים (תיקון באג). הוסר ה-override הזמני `?preview=1`+seed. **החלטה:** טיימרים נשארים אוטומטיים; חובה לתקן במקביל את באג הטיימרים ב-AI (backlog). שלבים 2+4 טרם נסקרו. פירוט ב-`ux_ui_v0.1.md`. | פתוח (branch, טרם בפרודקשן) |
| 2026-07-08 | 🧭✨ | UX/UI | **כלל גלובלי: שני הדרים בכל עמוד + AddRecipe שלב 1 עוצב** | על branch `ux-redesign-preview`. **נקבע כלל גלובלי:** כל עמוד באפליקציה חייב הדר עליון (לוגו MATKON + חץ + קופי ממורכז) והדר תחתון (`BottomNav`), למעט מצב בישול (תמונת מתכון למעלה) והעמוד הראשי. **AddRecipe שלב 1 (קלט):** נוספו שני ההדרים ("הוספת מתכון חדש"); **סרגל התקדמות חדש** — פס מקוטע דק סגול במקום עיגולים ממוספרים גדולים ("המספרים גנבו פוקוס") + תיקון באג key; **טאבי קלט צבעוניים** כמו Peek (הקלדה=כחול/לינק=צהוב/צילום=סגול) + יישור אייקונים; כפתור "יאללה" — אייקון אש הוסר + כחול-גלוס; תוקן חיתוך לוגו (`padding-top` ל-`.add-page`). שלבים 2-4 טרם נסקרו. פירוט ב-`ux_ui_v0.1.md`. | פתוח (branch, טרם בפרודקשן) |
| 2026-07-08 | ✨ | UX/בישול | **CookingMode — עוצב מחדש במלואו: הדר מלא כמו עמוד המתכון + סרגל תחתון + זהות ירוקה לטיימר + "שלב N" סגול** | על branch `ux-redesign-preview`. **הדר:** הוחלף מ-topbar קומפקטי ל-hero מלא — תמונה פרוסה + שם המתכון + כפתורי פעולה **פעילים** (לב/שמירה/שיתוף, לוגיקה משוכפלת מ-RecipePage: כותב ל-`likes`/`saved`+toast, שיתוף native/clipboard). הרציונל: משתמש עשוי לרצות לשמור/לאהוב/לשתף באמצע בישול. **סרגל תחתון (`BottomNav`) נוסף** + `page-with-nav` — מעבר מהיר למתכון אחר. **טיימר:** זהות ירוקה (רקע/מסגרת/ספרות), נבחר אחרי השוואת סגול/צהוב/כחול. **"שלב N":** סגול בולט (`#a78bff`), נבחר אחרי השוואת צהוב/ירוק. **checkbox** אומת שעובד (וי ירוק + קו-חוצה + התקדמות אוטומטית). **התמדה** אומתה (done/activeStep/timers ב-localStorage; טיימרים ממשיכים בזמן שבחוץ). כולל את התראת סיום הטיימר (צליל+רטט+חיווי ירוק, פעם אחת) שנוספה קודם. אומת חי, אפס שגיאות. | פתוח (branch, טרם בפרודקשן) |
| 2026-07-08 | ✨ | UX/בישול | **CookingMode — התראת סיום טיימר (צליל + רטט + חיווי ויזואלי)** | על branch `ux-redesign-preview`. עד היום בסוף טיימר שלב היה רק חיווי ויזואלי שקט ("✓ הסתיים"). נוסף `timerAlarm()`: (1) 3 צפצופים עולים 880Hz דרך Web Audio (מותר לנגן כי לחיצת "הפעל" היא מחוות משתמש שמשחררת אודיו); (2) רטט `navigator.vibrate([300,150,300,150,300])` (אנדרואיד; iOS מתעלם); (3) חיווי ויזואלי בולט — "⏰ נגמר הזמן!" ירוק במשקל 800 + מסגרת/רקע ירוקים + פעימה ×3 (`timer-done-pulse`). **פעם אחת בלבד** (החלטת משתמש). אומת חי (טיימר-בדיקה של 3ש' → הגיע ל-0, חיווי הופיע, אפס שגיאות קונסול). שאר CookingMode (צבעים/כפתורי גלוס/checkbox) פתוח לסשן הבא. | פתוח (branch, טרם בפרודקשן) |
| 2026-07-08 | ✨🔒 | UX/UI + הרשאות | **RecipePage — תצוגת מבקר לא-מחובר ("קיר הצטרפות") מומשה ואושרה** | המשך על branch `ux-redesign-preview` (**לא בפרודקשן**). מבקר שהגיע דרך לינק משותף בלי חשבון (`isGuest = !currentUser`) רואה את המתכון אבל **כל פעולה נעולה**: לב/שמירה/שיתוף/"התחילו לבשל"/"הוסיפו לרשימת הקניות"/הסרגל התחתון/תיבת התגובה — מעומעמים, ובלחיצה נפתח **פופ-אפ קיר-הצטרפות** ("רק רגע — צריך חשבון" + "הרשמה למטבח"→/register + "כניסה"→/login). **אכיפה תכנותית, לא רק ויזואלית:** כל handler בודק `isGuest` ומונע ביצוע; אומת חי שקליק על "התחילו לבשל" פותח פופ-אפ ולא מנווט. `BottomNav` קיבל props `locked`+`onLocked`. **🐛 תיקון עמעום:** אסור `opacity` על הסרגל/עיגולים (הופך את עיגול "מתכונים" הבולט לשקוף → טקסט בגלילה נראה מבעדו); במקום — בר אטום מלא (`#0a1123`) + שכבת כחול-כהה שקופה-למחצה מעל העיגולים + צבע מוחלש על הטקסט. `page-with-nav` נוסף ל-RecipePage. **🧭 החלטה:** צ'יפ "נדיר" (חיפוש חנויות למצרך נדיר) הוסר מעמוד המתכון — שייך רק לרשימת הקניות; הוסר גם הקוד המת. פירוט מלא ב-`ux_ui_v0.1.md`. | פתוח (branch, טרם בפרודקשן) |
| 2026-07-07 | 🧭 | UX/UI | **סבב עיצוב UX/UI (מובייל) — Feed + RecipePage (תצוגת חבר) + החלטת מוצר "קיר הצטרפות" למבקר** | המשך על branch `ux-redesign-preview` (**לא בפרודקשן**). **Feed:** header אחיד (לוגו+חץ+פעמון), **סרגל תחתון חדש** — "מתכונים" במרכז ככדור מורם ירוק-גלוס (היעד הראשי), "הוספה" ככדור סגול-גלוס בצד, הוסר ה-FAB הירוק הכפול; כרטיס מתכון בלי תגית קטגוריה, תצוגת משתמש קנונית (דגל מדינה→ישראל, שם+אות ראשונה), שורת stats אחידה (♡/💬/⏱ תמיד, 0 כשאין); קופי חיפוש; **מקור הפיד** = מתכוני קהילה בהחרגת מתכוני המשתמש עצמו (`neq user_id`), מהחדש לישן. **RecipePage — תצוגת החבר אושרה:** בלי לוגו (עמוד תוכן, לא onboarding), תצוגת משתמש קנונית + הסרת שורת מיקום, הסרת פרומפט "הצטרפו לקהילה כדי להגיב", כפתור "הוסיפו לרשימת הקניות" צהוב-גלוס והועבר מעל התגובות, כפתור שליחת תגובה בגובה מלא, כותרת "מצרכים" ממורכזת מול +/−. אומת שכל אייקוני הפעולה מחוברים לדאטה אמיתית. **🧭 החלטת מוצר גדולה — מבקר לא-מחובר ("מתכון + קיר הצטרפות"):** מבקר מלינק משותף בלי חשבון יראה את המתכון אבל **לא יוכל לבצע שום פעולה** — כל הכפתורים/הסרגל התחתון עמומים, ולחיצה → פופ-אפ "יש להירשם קודם" + לינק. **טרם מומש** — לסשן הבא (לוודא שעובד תכנותית, לא רק ויזואלית). פירוט ב-`ux_ui_v0.1.md`. | פתוח (branch, טרם בפרודקשן) |
| 2026-07-07 | 🧭 | UX/UI | **סבב עיצוב UX/UI (מובייל) — המשך: מסכי SSOCountry, CompleteProfile, Login ו-VerifyEmail אושרו** | המשך העבודה על branch `ux-redesign-preview` (**לא בפרודקשן**, לא בוצע commit). כלל שנקבע: **לוגו MATKON + חץ חזרה בראש כל מסך** (תבנית `.auth-top`, חוץ מ-Splash). **SSOCountry:** לוגו בראש (אייקון מיקום הוסר), `datalist` שבור→`CountrySelect`, כפתור כחול-גלוס, טקסט טיפ עודכן (שמות מצרכים בשפה מקומית + מצרכים נדירים). **CompleteProfile:** לוגו בראש (אמוג'י שף הוסר), label חדש ("ספרו לנו במשפט על עצמכם. לא חובה..."), כפתור "כניסה לקהילה" ירוק-גלוס בטקסט לבן, קישור דילוג הוסר. **Login:** "שלום שוב 👋"→"שמחים שחזרתם" (בלי אמוג'י), כפתור כחול-גלוס, "שכחתם סיסמה?" ריווח. **VerifyEmail:** לוגו בראש (אמוג'י תיבת-דואר הוסר), "מייל"→"Email", כותרת+טקסט מסודרים לשורות, כפתור "לא קיבלתי, שלחו שוב" אדום-גלוס. **שינוי צבע גלובלי:** טקסט כפתור ירוק-גלוס → לבן (במקום ירוק כהה). מעקב החלטות מלא ב-`ux_ui_v0.1.md`. הבא בתור: Feed. | פתוח (branch, טרם בפרודקשן) |
| 2026-07-07 | 🐛 | כניסה/Auth | **תוקן: הודעת שגיאה מטעה בכניסה כשהמייל לא אושר** | אומת בנתוני אמת ש-Supabase "Confirm email" דלוק (2/2 משתמשים מאושרים ב-`auth.users`) וכניסה בלי אישור מייל חסומה כראוי. אבל `Login.jsx` הציג לכל שגיאת התחברות את ההודעה הגנרית "Email או סיסמה שגויים" — כך שמשתמש עם סיסמה נכונה אך מייל לא-מאושר קיבל הודעה שגויה (חשב שטעה בסיסמה). תוקן: זיהוי `error.code === 'email_not_confirmed'` (או `/confirm/i` בהודעה) → מציג "צריך לאשר את המייל לפני הכניסה. שלחנו לכם לינק — בדקו את תיבת הדואר." + שומר `pending_email`. אומת שהמסך מתרנדר תקין בלי שגיאות קונסול. על branch `ux-redesign-preview`. | פתוח (branch, טרם בפרודקשן) |
| 2026-07-07 | 🧭 | UX/UI | **סבב עיצוב UX/UI (מובייל): מסכי Splash, Peek ו-Register אושרו + מערכת עיצוב חדשה** | עבודה על branch `ux-redesign-preview` (**לא בפרודקשן**, לא בוצע commit). נקבע ונעול: מערכת צבעים "גלוס-לייט" מאוחדת (5 גוונים כחול/סגול/ירוק/צהוב/אדום + סקאלות בהיר/בסיס/כהה + מפרט כפתור, רדיוס 18px), פונט **Rubik** לגוף/UI (הלוגו נשאר Assistant), והגדלת טקסט גלובלית ל-118%. **Splash:** סדר כפתורים הפוך (כניסה=סגול/הרשמה=כחול), קופי, סלוגן+קישור במשקל/גודל. **Peek:** 4 שקופיות per-אספקט (כרטיס מתכון / 3 אייקוני העלאה צבעוניים / מוקאפ רשימת קניות / מוקאפ מצב בישול), ניווט "הבא"+"הקודם"+נקודות, ותצוגת משתמש קנונית (דגל מדינה+דגל ישראל+שם פרטי+אות ראשונה, בלי עיר). **Register:** לוגו+כותרת ממורכזים, חץ חזרה נקי ממורכז לפונט הלוגו, הסרת אימות-סיסמה+סלוגן, כפתור גלוס, SSO בסטנדרט מותגי (Google+Apple לבנים עם לוגו רשמי — לוגו אפל מ-`Brief/logos_Icons`), "אימייל"→"Email", ורכיב חדש `CountrySelect` שמחליף `datalist` שבור. מעקב החלטות מלא ב-`ux_ui_v0.1.md`. הבא בתור: SSOCountry/CompleteProfile. | פתוח (branch, טרם בפרודקשן) |
| 2026-07-05 | 🔒 | אבטחה/Auth | **הופעל Leaked Password Protection על שני הפרויקטים (matkon + matkon-admins)** | היה חסום על התוכנית החינמית; לאחר שדרוג ה-Supabase ל-Pro (5.7) נפתח. הופעל ידנית ב-Dashboard בשני הפרויקטים: Authentication → Providers → Password Security → "Prevent use of leaked passwords" (מבוסס HaveIBeenPwned Pwned Passwords API) → Save. מעכשיו סיסמאות שדלפו נחסמות בהרשמה/החלפת סיסמה — גם למשתמשים וגם לאדמינים (הגנה-בשכבות בנוסף למדיניות החזקה + MFA של האדמין). | פרוס |
| 2026-07-03 | 🐛 | קהילה | **תוקן: סינון הקהילה לפי מדינה לא עבד (אותו שורש כמו באג הלוקליזציה)** | `Recipes.jsx` — `COUNTRY_FILTERS` השתמש בקודים באנגלית ("Germany") מול `users.country` ששמור בעברית ("גרמניה") → בחירת מדינה לא סיננה כלום וברירת-המחדל לפי מדינת המשתמש לא נקבעה. תוקן: קודים עבריים התואמים לערך המאוחסן. **באג שני שהתגלה תוך כדי:** `.eq('users.country', code)` על טבלה משובצת רק מאפס את פרטי המחבר הלא-תואם — לא מסיר את שורת המתכון; תוקן ל-`users!inner(...)` כשיש סינון. אומת בפריוויו: גרמניה→"חומוס ביתי" (מיכל כהן) בלבד, צרפת→"סלט ישראלי", ארה"ב→"שקשוקה", כולם→הכל. commit `ebd5a34`. **באג נוסף (נפרד) שנמצא ותוקן:** טאב הקהילה רינדר כל מתכון פעמיים (אזהרת duplicate-key) — ה-observer של ה-infinite-scroll ירה שוב `loadCommunity` לעמוד שכבר נטען (אומת ב-network: 2 קריאות זהות offset=0) והוסיף את אותן שורות. תוקן: מיזוג לפי `id` ב-setter (dedupe). אומת: מעבר חוזר לטאב נשאר 5 כרטיסים ייחודיים, אפס אזהרות. commit `dac70b2`. | פרוס |
| 2026-07-03 | 🐛 | קניות/מתכון | **תוקן: שמות מצרכים ברשימת הקניות הוצגו בעברית במקום שפת-המקום, וחיפוש "איפה למצוא" לא מצא חנויות** | דיווח משתמש (ניו זילנד): המצרכים הוצגו שוב בעברית (לא באנגלית), וחיפוש חנויות ל"קמח שקדים" החזיר "לא נמצאו חנויות" למרות שזה בכל סופר. **שורש (אחד לשתי התקלות):** `country` נשמר בעברית בכל המערכת ("ניו זילנד") ומעולם לא הומר — נשלח כמו-שהוא ל-Haiku, אז name_local חזר בעברית, וה-web-search חיפש את המונח **העברי** בחנויות NZ. **הבהרה:** בעבר זה כן עבד באנגלית (ב-16.6 אף עבדו על צבע הפונט של השם הלועזי) — הפרומפט הישן סמך על Haiku *להסיק* ש"ניו זילנד"=אנגלית, וזה עבד רוב הזמן אבל **אינטרמיטנטית**; על מתכון זה (link-import עם שמות מעורבים) המודל נפל לעברית. התיקון הופך ניחוש→ודאות. (ה-E2E של #19 עקף במקרה את שני הבאגים כי נבדק רק עם מצרך באנגלית.) **תיקון:** `api/_countries.js` (חדש) — מיפוי מדינה עברית→{אנגלית, שפה}; `translate-ingredients` מתרגם לשפת-היעד ואוסר עברית; `find-rare-ingredient` מחפש עם שם המדינה באנגלית + מתרגם את המצרך לפני החיפוש; `Shopping.jsx` מתרגם-מחדש פריט עם name_local עברי. **🐛 באג שני שהתגלה תוך ה-E2E:** גם אחרי התיקון החיפוש החזיר [] — הפרסר עשה `JSON.parse` על כל טקסט התשובה, אבל Haiku עוטף את ה-JSON בפרוזה אחרי web_search → זריקה → אובדן כל התוצאות (#19 עבד במקרה כי התשובה היחידה הייתה נקייה). תוקן: חילוץ מערך ה-JSON ב-regex. **אומת E2E חי בפרודקשן** (משתמש QA, נוקה): תרגום → almond flour/egg whites/icing sugar (מונחי NZ נכונים); "קמח שקדים" → 3 חנויות NZ אמיתיות (Bulk Barn/Woolworths/PAK'nSAVE); "רכז רימונים" → 3 חנויות + cache-hit בקריאה שנייה. commits `9472608`+`50f2273`. | פרוס וחי |
| 2026-07-03 | 🐛 | פרופיל | **תוקן: טאב "שמורים" בעמוד הפרופיל היה placeholder ריק קבוע — עכשיו טוען מה-DB** | המשך הפער שזוהה ב-2.7: `Profile.jsx` הציג תמיד "עדיין אין מתכונים שמורים" ולא טען כלל מהטבלה (בעוד ספירת "שמורים" בכותרת דווקא עבדה). תוקן לפי הדפוס המאומת ב-`Recipes.jsx` (`loadSaved`): שאילתת `saved`→`recipe_id`s→`recipes`, נקראת ב-`useEffect` הקיים אחרי שה-auth נטען, עם state `saved`+`savedLoading` (טעינה/ריק/רשימה כמו טאב "המתכונים שלי"). חולץ רכיב `ProfileRecipeItem` ושני הטאבים משתמשים בו — זהה ויזואלית לרשימת "המתכונים שלי", בלי כפילות קוד. עמוד "מתכונים" לא נגעו בו. **אומת E2E חי מול פרודקשן** (משתמש QA זמני ששמר 2 מתכונים, נוקה במלואו): הטאב הציג את שני המתכונים עם תמונות/קטגוריה/זמן/תג "משותף", בלי שגיאות קונסולה. | פרוס |
| 2026-07-02 | 🐛 | עמוד מתכון | **תוקן: כפתורי לייק/שמירה על תמונת המתכון "לא עובדים" — מצב הכפתור לא נטען בכניסה לעמוד** | דיווח משתמש. האבחנה (שוחזר במלואו עם משתמש QA זמני, נוקה): הלחיצות עצמן דווקא נשמרו ב-DB (201), אבל בטעינה טרייה של העמוד `loadRecipe` רץ לפני שה-auth הסתיים ובדק את המשתמש מ-render ראשון (ריק) → `loadLike`/`loadSaved` מעולם לא רצו → הלב/סימנייה תמיד הוצגו ריקים אחרי רענון, מה שנראה כאילו הלחיצה "לא עשתה כלום". תוקן: טעינת liked/saved עברה ל-effect ייעודי שתלוי גם ב-user וגם ב-id. בנוסף: (א) שגיאות insert/delete בכפתורים כבר לא נבלעות — toast "שגיאה, נסו שוב", ו-23505 (כבר-קיים ממכשיר אחר) מטופל כהצלחה; (ב) **פער DB שהתגלה אגב: לטבלת `likes` לא היה UNIQUE על (user_id,recipe_id)** (ל-saved יש) — לחיצות חוזרות היו יוצרות לייקים כפולים ומנפחות ספירות. migration `20260702120000_likes_unique_user_recipe` (dedupe + constraint, הוחל; אפס כפילויות היו בפועל). אומת ב-preview מקצה לקצה: לייק+שמירה → רענון → מצב מלא נשמר → הסרה → DB התרוקן. **פער המשך שזוהה באותה בדיקה (לתיקון בסשן הבא):** טאב "שמורים" בעמוד הפרופיל הוא placeholder ריק קבוע — אף פעם לא טוען מה-DB (Profile.jsx); עמוד "מתכונים" תקין ומציג שמורים/אהובים נכון. | פרוס |
| 2026-07-02 | ✨🔒 | כניסה/אבטחה | **#18 Passkeys (WebAuthn) — כניסה עם Face ID/טביעת אצבע בלי סיסמה, בשיטת autofill (בלי כפתור)** | הרקע: המשתמש נדרש להקליד מייל+סיסמה בכל כניסה מהאייפון (iOS מנקה localStorage), בעוד שבמקבוק Keychain ממלא אוטומטית אחרי טביעת אצבע. הפתרון בשתי שכבות: **(א) תגיות autocomplete תקניות** על טפסי Login/Register (`username webauthn`, `current-password`, `new-password`, `given/family-name`) — מחזיר את הצעת המילוי של סיסמאות שמורות גם באייפון; **(ב) Passkeys ב-Conditional UI** — התכונה הרשמית של Supabase Auth (experimental, supabase-js ≥2.105): בעמוד הכניסה נחמשת בקשת WebAuthn שקטה, ואם יש passkey למכשיר — המערכת מציעה אותו מעל המקלדת בנגיעה בשדה האימייל → Face ID → בפנים, בלי סיסמה בכלל. **החלטת עיצוב של המשתמש:** בלי כפתור ייעודי "כניסה עם Face ID" — רק autofill (כמו Google/GitHub). רישום מפתח: מודאל חד-פעמי אחרי כניסה עם סיסמה ("כניסה מהירה בפעם הבאה") + כרטיס ניהול מפתחות בפרופיל→פרטיות ונתונים (רשימה/הוספה/הסרה). מגבלות ידועות: משתמשי Google SSO לא יכולים לרשום passkey (מגבלת Supabase; ממילא נכנסים בלי סיסמה); RP ID=`matkon.co` נקבע ב-Supabase Dashboard והוא החלטה קבועה (שינוי מבטל את כל המפתחות). קבצים: `src/lib/passkeys.js` (חדש — ceremony דו-שלבי + conditional mediation), `src/lib/supabase.js` (opt-in + helpers), `Login.jsx`, `Register.jsx`, `Profile.jsx`, `PasskeyOfferModal.jsx` + `PasskeySection.jsx` (חדשים). אין שינויי DB/שרת. הגדרת Dashboard בוצעה (Passkeys enabled, RP ID matkon.co, origins matkon.co+www). **אומת E2E חי ע"י המשתמש מהאייפון (2.7.26):** כבר בפתיחת עמוד הכניסה iOS הציע מילוי אוטומטי עם Face ID (תיקון ה-autocomplete), אחרי הכניסה הופיע מודאל ההצעה, הרישום עבד (iOS הציע בחירת מיקום שמירה למפתח). | פרוס וחי |
| 2026-07-01 | ✨ | מתכון/קניות | **#19 מציאת מצרכים נדירים — web search אמיתי + cache, במקום ניחוש AI. פרוס וחי, אומת E2E** | לפי הבריף (P1): ליד מצרך "נדיר" (בעמוד מתכון וברשימת הקניות) יש עכשיו חץ שבלחיצה פותח dropdown עם 2-3 חנויות אמיתיות + לינקים קליקביליים — במקום הטקסט הכללי הקיים (`where_to_buy` שנוצר מניחוש כללי של Claude, בלי לינקים, בלי אימות). **חדש:** `api/find-rare-ingredient.js` — endpoint מאומת, rate-limit 15/שעה, budget guard, קורא ל-Claude Haiku 4.5 עם כלי `web_search` (תוצאות חיפוש אמיתיות, לא מומצאות), מסנן תוצאות ל-URL תקין בלבד, כותב usage_log (טוקנים + $0.01/חיפוש). טבלת cache חדשה `rare_ingredient_stores` (migration `20260701130000`) לפי (מצרך, מדינה) — לחיצה חוזרת על אותו מצרך היא קריאת DB חינמית, לא קריאת AI חדשה. נקרא **רק בלחיצה** של המשתמש (לא אוטומטית לכל מצרך נדיר) לחיסכון בעלות. `translate-ingredients.js` (מסמן `is_rare`) נשאר ללא שינוי. **UI:** `RecipePage.jsx` — כפתור `נדיר` ליד כל מצרך פותח dropdown ממוקם; הוסר הקופסה המרוכזת הישנה. `Shopping.jsx` — אותו רכיב ליד פריטים עם `where_to_buy`. אומת ב-preview עם משתמש+מתכון QA זמניים (נוקו): הכפתור, הצ'בְרון, וה-fallback הנקי כש-`/api` לא זמין (dev מקומי) — עובדים כמצופה. **🐛 באג שנמצא ותוקן ב-E2E חי (commit `d33e248`):** כתיבת ה-cache הייתה fire-and-forget (`adminInsert` בלי `await`) — Vercel הרג את ה-promise ברגע שה-response נשלח, אז שורת ה-cache **אף פעם לא נכתבה**, כלומר כל לחיצה חוזרת הייתה קוראת ל-AI מחדש במקום להיות קריאת DB חינמית (בדיוק הבעיה שה-cache נועד למנוע). תוקן ל-`await`. **אומת E2E חי ב-matkon.co**: קריאה ראשונה ("pomegranate molasses"/NZ) החזירה 3 חנויות NZ אמיתיות (Vetro/Sabato/The Grocer) עם קישורי מוצר תקינים; קריאה שנייה זהה החזירה `cached:true` תוך פחות משנייתיים, בלי חיוב AI נוסף. כל ארטיפקטי ה-QA (2 משתמשים, שורות cache, usage_log) נוקו. | פרוס וחי |
| 2026-07-01 | 🐛 | DB/רישום משתמשים | **קריטי: תוקן טריגר `handle_new_user_security` שבר את כל ההרשמות בפרודקשן** | התגלה תוך כדי הכנת בדיקת E2E ל-#16: הטריגר עדיין הכניס `role` ל-`user_security` וקרא מ-`public.admin_bootstrap` — שניהם הוסרו מפרויקט A בסשן הפרדת-DB האדמין (29.6 ערב) בלי לעדכן את הטריגר. מכיוון שזה AFTER INSERT על `auth.users`, שגיאה בו מפילה את כל טרנזקציית ה-signup → **כל הרשמה חדשה (מייל או גוגל) נכשלה** מרגע הניקוי. אף אחד לא נתקל בזה כי שני החשבונות הקיימים נוצרו לפני. תוקן: `handle_new_user_security` עכשיו רק יוצר שורת `user_security(id)` ריקה (ל-strikes/banned defaults), בלי role/bootstrap. migration `20260701120000_fix_handle_new_user_security_post_split.sql`. אומת ע"י יצירת+מחיקת משתמש בדיקה. | פרוס (DB) |
| 2026-07-01 | 🐛 | DB/הרשאות | **קריטי: `saved` ו-`recipe_comments` חסרו GRANT INSERT/DELETE ל-authenticated — שמירת מתכון ותגובות שבורות בפרודקשן** | התגלה תוך כדי E2E ל-#16 (שמירת מתכון כמשתמש B נכשלה עם `42501 permission denied`). שתי הטבלאות היו RLS-policies תקינות (`saved_insert/delete`, `comments_insert/delete` לפי `auth.uid()=user_id`) אבל בלי ה-GRANT הבסיסי — Supabase/PostgREST דורש את שניהם. **המשמעות בפועל:** כל לחיצה על "שמור מתכון" של מתכון-של-מישהו-אחר וכל שליחת תגובה נכשלו בשקט (RecipePage.jsx בולע את השגיאה, לא מציג הודעה למשתמש) — כנראה שבור מאז יצירת הטבלאות (לא רגרסיה מ-29.6). תוקן: `GRANT INSERT, DELETE ON saved, recipe_comments TO authenticated`. migration `20260701121000_fix_saved_comments_grants.sql`. אומת חי: B שמר מתכון בהצלחה אחרי התיקון. | פרוס (DB) |
| 2026-07-01 | ✨ | התראות | **#16 E2E אומת חי בפרודקשן — התראת "מתכון נמחק" עובדת מקצה לקצה** | נבנו 2 משתמשי QA זמניים, פורסם מתכון אמיתי (עבר moderation), נשמר ע"י המשתמש השני, נמחק ע"י היוצר — ואומת שההתראה הגיעה ל-DB עם payload נכון (כותרת/שם-יוצר/מדינה) ושקריאת ה-RLS-scoped שהפעמון עושה (`notifications?read_at=is.null`) מחזירה unread=1. כל הארטיפקטים (משתמשים, התראה) נוקו בסוף. | בוצע |
| 2026-06-30 | 🔒 | אבטחה/מתכון | **#17 חיזוקים סופיים — Sonnet לוויז'ן, דה-דופ קצר, ספירת junk, תיקון rate-limit** | המשתמש זיהה נכון 2 חורים: (א) קריקטורה של איש עירום סווגה כ-junk במקום abuse (Haiku ראייה חלשה); (ב) אותה תמונה שנדחתה בלולאה = הטרלה. **4 שינויים:** (1) `_moderation.js`: `callModeration` מזהה אוטומטית תמונה במסר → Sonnet, טקסט בלבד → Haiku. `PRICING` per-model לרישום עלות נכון. (2) `_moderation.js`: חלון דה-דופ קוצר מ-24ש' ל-60 שניות — עדיין מגן על double-click לגיטימי, אבל לא מתיר טרול שמנסה שוב אחרי דקה. (3) `moderate-image.js`: `endpoint` עובר עכשיו דרך moderateRawInput→callModeration → נכתב כ-'moderate-image' ב-usage_log → ה-rate limit 10/שעה **סוף סוף אוכף** (היה שבור — שם endpoint שלא נכתב). (4) `moderate-image.js`: `counts: true` (במקום `mod.verdict.kind === 'abuse'`) — user-uploaded image = מעשה ישיר של המשתמש, גם junk סופר ב-junk_strikes (ban ב-5). ban ב-3 עדיין לאלימות/עירום/דגלים/גזענות. Sonnet יסווג נכון עירום גם בקריקטורות → strikes אמיתי. **נוקו אגב 5 רשומות moderation_log מבדיקות המשתמש היום.** | בלוקאלי |
| 2026-06-30 | ✨ | UX/אבטחה | **#17 המשך — מודאל אזהרה מרכזי לתמונה שנדחתה + סגירת פער ב-EditRecipe** | המשתמש בדק חי את #17 ומצא 2 בעיות: (א) ב-RecipePage ה-toast הקטן בתחתית "נבלע" — לא צועק, וגם הנוסח שגוי ("לא זיהינו מתכון" — מתאים לטופס מתכון לא לבחירת תמונה); (ב) ב-EditRecipe — כשבחר תמונה בעייתית דרך כפתור עריכה (עפרון) המערכת **לא הגיבה בכלל** וחזרה למתכון עם התמונה הישנה. **שורש הבעיה ב-EditRecipe:** הזרימה הייתה upload→storage→update-recipe שמודר טקסט+תמונה יחד; ה-AI ראה טקסט מתכון לגיטימי ונתן משקל יותר לטקסט מאשר לתמונה הפוגענית — verdict=allowed → אפסור 200 → navigate ל-/recipe. **תיקון:** (1) רכיב חדש `src/components/ImageRejectionModal.jsx` — מודאל מרכזי חוסם, רקע שחור 70%, כרטיס עם גבול אדום, אייקון `IconAlertTriangle` במעגל אדום, כותרת "התמונה לא מאושרת", גוף עם הנוסח שאישר המשתמש ("התמונה שבחרתם אינה עומדת בכללי הקהילה. בחרו תמונה של אוכל בלבד, בלי אנשים, טקסט, דגלים או סמלים פוגעניים."), כפתור אדום בולט "הבנתי", autoFocus, click-outside-to-close, escape-friendly. **CSS חדש ב-index.css**: `.image-reject-overlay/.image-reject-card/.image-reject-icon/.image-reject-title/.image-reject-body/.image-reject-btn` + 2 keyframes (fade-in + pop-in). (2) RecipePage `handleImageReplace` — 422 מציג כעת מודאל במקום toast; banned עדיין מנווט ל-/blocked; שאר השגיאות נשארו ב-toast (קוסמטי). (3) **EditRecipe save** — לפני העלאה ל-storage, pre-call ל-`/api/moderate-image` עם base64 (אותו endpoint+vision שתפס ב-RecipePage). בדיקת תמונה לבדה ממקדת את Haiku ומונעת את ההטיה של "טקסט תקין מצדיק את הכל". 422→מודאל; אם עבר→ממשיך עם upload+update-recipe הרגיל. גם אנטי-orphan-storage: תמונה שנדחתה לא נשמרת ל-bucket. **אומת חי ב-preview**: המודאל מוצג נכון (מרכזי, רקע מעומעם, RTL נקי, אייקון אדום בולט, נוסח מדויק, כפתור בולט), אין שגיאות קונסול. | בלוקאלי |
| 2026-06-30 | 🔒 | אבטחה/מתכון | **#17 מודרציית תמונה — כל תמונה עומדת בכללי הקהילה** (החלטת משתמש: AI=הקשחת prompt, user-upload=vision חובה) | **3 שינויים:** (1) [api/parse-recipe.js:158] פרומפט `image_search` הוקשח — "האוכל בלבד", אסור בני אדם/ילדים/פנים/ידיים/טקסט/דגלים/סמלים דתיים או פוליטיים; הוסר עיטור התיאור (עובר לשרת). (2) [api/publish-recipe.js:15] קבוע `POLLINATIONS_SAFETY_SUFFIX` משתרשר תמיד ל-`pollinationsUrl()` — "food only, plated dish, no people, no children, no faces, no hands, no text, no captions, no watermarks, no flags, no religious symbols, no political symbols, no logos". הקשחה ב-2 שכבות (parse + server-side) = defense in depth. תמונת AI לא עוברת vision check (חיסכון עלות) — הקשחת ה-prompt מפצה. (3) **🚨 ממצא קריטי באודיט:** הזיכרון אמר ש-`RecipePage.handleImageReplace` הוסר ב-#13, אבל בפועל הוא היה חי — `<input type=file>` ב-`RecipePage.jsx:391` קורא להעלאה ישירה ל-storage ועדכון `recipes.image_url` מהלקוח, **בלי שום מודרציה**. נסגר ב-endpoint חדש `/api/moderate-image.js` (auth+rate-limit 10/שעה+budget guard+`moderateRawInput` vision; abuse=strike, junk=block-only כי הטקסט עצמו תקין). `handleImageReplace` עכשיו: compress → base64 → POST /api/moderate-image → רק אם 200 OK ממשיך לupload+DB. נוסף גם cache-bust query (`?v=ts`) כדי שהתמונה החדשה תופיע מיד (אותו path נדרס). שאר ה-paths אומתו נקיים: AddRecipe→parse-recipe (vision Sonnet) + publish-recipe (vision URL fetch) ✅, EditRecipe→update-recipe (vision רק אם imageChanged) ✅. | בלוקאלי |
| 2026-06-30 | 🐛 | UX/הרשמה | **תיקון: CompleteProfile קפץ למשתמשים קיימים בלי bio** | באג שדווח ע"י המשתמש מיד אחרי deploy `9444f6e`: נכנס לחשבון קיים (מ-11.6) והקפיץ אותו ל-CompleteProfile כי `matkon_bio_prompt_seen` לא היה במכשיר (תפקד כמתוכנן ל-"ראשון במכשיר") אבל ה-bio שלו תמיד היה null (אופציונלי במקור). תיקון: הוספת בדיקת `fresh account` בשני המקומות (Login.jsx + App.jsx AuthCallback) — הפרומפט מוצג רק אם `Date.now() - new Date(user.created_at) < 10 דקות`. משתמש קיים שנכנס מקבל `seen=1` בשקט וממשיך לפיד. משתמש חדש (signUp לפני <10 דקות) רואה את המסך פעם אחת. | בלוקאלי |
| 2026-06-30 | ✨ | התראות | **#16 מערכת התראות — recipe_deleted** (בריף §244) | המתכון נמחק לחלוטין כשהיוצר מוחק (cascade קיים) אבל כל מי ששמרו מקבלים התראה. **DB:** migration `20260630120000_notifications.sql` הוחל ב-A — טבלת `notifications(id/user_id/type/payload jsonb/read_at/created_at)` עם CHECK על type=`'recipe_deleted'` (מורחב בעתיד), 2 אינדקסים (partial unread + DESC created), RLS self-read+self-update (לא delete), GRANT SELECT/UPDATE ל-authenticated. service-role עוקף ועושה insert. **API:** `delete-recipe.js` קודם שואב את כל ה-`saved.user_id` (פרט לבעלים), שולף שם+מדינה של היוצר, ועושה bulk insert ל-notifications **לפני** המחיקה (FK CASCADE על saved ימחק את היעדים אחרת). העטוף ב-try/catch — שגיאת insert לא חוסמת מחיקה. **UI:** `NotificationsBell.jsx` (חדש) — פעמון ב-feed top bar (משמאל ללוגו), badge אדום עם ספירת unread (count-exact head), polling כל 60 שניות. drawer פותח 50 אחרונים DESC + auto-mark-read on view. הודעה: "המתכון 'X' שמרת נמחק על ידי היוצר (🇮🇱 דגל-מדינה שם-פרטי)". "ניקוי הכל" = mark-all-read (RLS לא מאפשר delete-self). component מחזיר null אם אין user. | פרוס |
| 2026-06-30 | ✨ | UX/הרשמה | **#1 כפתור הצג/הסתר סיסמה ב-Register וב-Login** | פער בריף 173+174. נוסף `IconEye`/`IconEyeOff` של Tabler ב-2 שדות הסיסמה ב-Register ובשדה של Login, עם CSS `.input-wrap` + `.input-eye` ייעודי (אייקון בצד שמאל מבפנים, hover עם רקע עדין). אומת חי ב-preview: לחיצה על העין הראשונה הופכת `type=password→text` רק על שדה הסיסמה (לא על האימות). | בלוקאלי |
| 2026-06-30 | ✨ | UX/הרשמה | **#2 שדה וידוא אימייל ברישום** | פער בריף 172. נוסף `emailConfirm` עם בדיקת התאמה case-insensitive + הודעת ✓/✗, החסימה מתפעלת רק אחרי הקלדה בשני השדות. הוספתי `onPaste={e=>e.preventDefault()}` לשדה הוידוא כדי לכפות הקלדה אקטיבית (מונע "copy/paste type-o"). | בלוקאלי |
| 2026-06-30 | 🐛 | UX/פרופיל | **#3 סטטיסטיקות פרופיל אמת (לייקים/שמורים)** | היה hardcoded `0` בשני הסטטיסטים האחרונים. נוסף `loadCounts(userId)` שעושה 2 spaarrelles `count: 'exact', head: true` מקבילים (likes.user_id = userId, saved.user_id = userId) ומחליף את ה-0. עכשיו תואם בריף §39 ("כמה מתכונים אהב, כמה מתכונים כל משתמש העלה"). | בלוקאלי |
| 2026-06-30 | 🔒 | פרטיות/UI | **#4 הסרת אווטר-ראשי-תיבות ב-4 מקומות → דגלים+שם פרטי בלבד** | אכיפת כלל [[feedback_highest_security]] + [memory 19.6 "אין אווטרים"]: (1) `Profile.jsx` — `avatar-xl` עם 2 אותיות הוחלף ב-`.profile-flags` (2 דגלים גדולים 3rem עם drop-shadow) + שם פרטי בלבד; (2) `RecipePage.jsx` שורת מחבר — `avatar-md` הוסר, נשאר רק `🇮🇱 דגל-מדינה שם-פרטי`; (3) `RecipePage.jsx` תגובות — `avatar-sm comment-avatar` הוסר, נשאר רק `🇮🇱 דגל שם-פרטי + זמן`; (4) `Community.jsx` חברי קהילה — `avatar-sm` הוסר, פורמט אחיד. כל הופעות שם המשתמש כעת `split(' ')[0]` (שם פרטי בלבד). אומת חי במסך RecipePage. | בלוקאלי |
| 2026-06-30 | ✨ | חיפוש | **#5 הרחבת חיפוש Feed לפי שם משתמש, מדינה, קטגוריה** | פער בריף 188 ("מתכון, תגית, סגנון, אוכל, מדינה/קהילה, שם חבר קהילה"). היה client-side filter על title/description/tags בלבד. נוסף: lowercase normalize + חיפוש גם ב-`category`, `users.full_name`, `users.country`. placeholder עודכן ל-"חפשו מתכון, קטגוריה, תגית, מדינה או חבר קהילה...". אומת חי: חיפוש "נפתלי" סינן 4→1. ⚠️ מגבלה: עדיין client-side על העמוד הטעון (PAGE_SIZE=12); חיפוש עמוק חוצה-עמודים = שדרוג עתידי (server-side full-text). | בלוקאלי |
| 2026-06-30 | 🧹 | UX | **#8 ניקוי `pending_email` מ-localStorage על SIGNED_IN** | היה שמור לתמיד אחרי register, גם אחרי שהמשתמש כבר התחבר. נוסף ל-`AuthContext.onAuthStateChange` בלוק שמוחק את המפתח ב-event === 'SIGNED_IN'. ניקוי קוסמטי + הסרת רעש מה-localStorage. | בלוקאלי |
| 2026-06-30 | ✨ | UX/מתכון | **#9 תמיכה בכמויות שבריות בעת שינוי מספר סועדים** | היה באג: `parseFloat("1/2")` החזיר 1 → "חצי כוס" הפכ לפעמיים, "רבע" הופך ל-1. נוסף `parseQty()` שמכיר ב-`1/2`, `1 1/2`, `1.5`, `½`/`¼`/`¾`/`⅓`/`⅔`/`⅛`/`⅜`/`⅝`/`⅞`, פסיק כנקודה עשרונית, ומיקס שלם+שבר. `fmtQty()` גם ממיר חזרה: אם התוצאה נופלת על שבר נפוץ → אייקון Unicode (`1½` במקום `1.5`); אחרת tofixed(1). הקוד עצמאי, לא תלוי בספרייה חיצונית. | בלוקאלי |
| 2026-06-30 | ✨ | קניות | **#10+#11 שיפוץ רשימת הקניות — קטגוריות + ספריית מחוקים** | סגירת 2 פערים מהבריף בבת אחת (§247+§249). **קטגוריות:** הפריטים מקובצים תחת 9 קטגוריות (`SHOPPING_CATEGORIES`): 🥗 ירקות, 🍎 פירות, 🥩 בשר ודגים, 🥛 מוצרי חלב, 🍞 מאפים, 📦 יבש וקופסאות, 🌶️ תבלינים, ❄️ קפואים, 🛒 אחר. סדר ההצגה לפי flow של חנות. סיווג: heuristic עברי מקומי `categorizeIngredient(name)` עם ~120 מילות מפתח (ייחודי לראשונה: "פלפל שחור"→spices לפני "פלפל"→produce_veg); ה-AI ב-`translate-ingredients` מחזיר גם `category` שמעדכן retroactively. **ספריית מחוקים:** במקום `removeChecked` שמחק לתמיד — `moveCheckedToDeleted` מעביר ל-`matkon_shopping_deleted` ב-localStorage. UI: כפתור מקווקו "ספריית מחוקים (N)" → drawer עם 🔄 שחזור / 🗑 מחיקה לתמיד. `clearAll` "ניקוי הרשימה" גם הוא הפך לארכוב (לא מחיקה). **משחזור אוטומטי**: אם הוספת מצרך ממתכון חדש כאשר אותו פריט כבר בספרייה → אוחזר אוטומטית (בריף §249 "...או יתווסף שוב ממתכון"). אומת חי E2E: סימון פריט → ארכוב → ספרייה → שחזור → חזרה לרשימה הפעילה. | בלוקאלי |
| 2026-06-30 | 🔒 | אבטחה/מתכון | **#13 הסרת fallback Pollinations מ-RecipePage hero** (סגירת פתח מודרציה) | היה: אם `recipe.image_url` ריק → ה-client בנה Pollinations URL מקומי, טען אותו, ושמר ל-DB ב-`saveAiImage()`. השער של moderation עוקף לחלוטין. ה-publish-recipe כבר בונה את ה-URL **אחרי** moderation server-side (סשן 23.6) → הקוד הזה היה שריד. תוקן: לסמוך על `recipe.image_url` בלבד; אם ריק → ה-gradient הקטגוריה (כבר ב-`background`). הוסרו גם `IconCamera` "מייצר תמונה..." overlay והכפתור "החלף תמונה" (handleImageReplace) שגם עקפו moderation. | בלוקאלי |
| 2026-06-30 | ✨ | UX/תגובות | **#14 timeAgo בתגובות מתעדכן חי** | היה: `timeAgo(c.created_at)` נחשב פעם אחת ב-render — "עכשיו" נשאר "עכשיו" עד refresh. נוסף `setTick` עם `setInterval(60_000)` ב-`RecipePage` שמכריח re-render דקה. עלות: שום. | בלוקאלי |
| 2026-06-30 | ✨ | הרשמה | **#15 CompleteProfile מחובר לזרימת הכניסה הראשונה** | בריף §182 ("אחרי כניסה למערכת, בפעם הראשונה ... חלון אפשרות להזנה של משפט קצר"). המסך היה קיים אבל אף route לא נווט אליו אחרי email signup. נוסף flag `matkon_bio_prompt_seen` ב-localStorage (per-device, חד-פעמי). **AuthCallback של SSO** (App.jsx): אחרי country+tos → אם `!bio && !seen` → `/complete-profile`. **Login** (אחרי email signin): אותה לוגיקה. **CompleteProfile** save+skip שניהם מסמנים seen=1 ולא נצמדים. תואם לדרישת "לא חובה" + "לא לנדנד יותר מפעם". | בלוקאלי |
| 2026-06-29 | ⏳ | אבטחה/ציבורי | **Leaked Password Protection — נדחה: דורש Supabase Pro** | ניסינו להפעיל ב-Authentication → Attack Protection → "Prevent use of leaked passwords" של פרויקט A. הופיעה שגיאה: "Configuring leaked password protection via HaveIBeenPwned.org is available on Pro Plans and up". זה היה מתועד בעבר ("ידני ב-dashboard: Leaked Password Protection; שדרוג Pro לקראת השקה") — עכשיו אומת חי. **נדחה לשדרוג Pro לקראת השקה** (Pro מוסיף גם גיבוי auth+storage מנוהל + PITR + custom domain — ערך כולל). | נדחה |
| 2026-06-29 | 📒 | תיעוד | **dashboard_Admin v1.2.docx — תיעוד עדכני לאדמין** | קובץ אפיון לוח הניהול עודכן מ-v1.1 (25.6) ל-v1.2 (29.6). נוסף פרק 13 שמסכם את שלושת אבני הדרך מאז: (13.1) יומן ביקורת בדשבורד — סקציית "פעולות מנהלים אחרונות"; (13.2) חיבור עלות AI אמת (סעיף 12.4 עבר מתוכנית לבוצע, עם הסבר על הגילוי 7× לעומת ההערכה ב-טוקנים); (13.3-13.4) הפרדת DB מלאה — שני פרויקטי Supabase A (ציבורי) ו-B (אדמין), עם הסבר זרימה ומי קורא מאיפה; (13.5) עדכוני סעיף 10 (בסיס נתונים) לארכיטקטורה החדשה; (13.6) שלוש סוגיות זוהו לטיפול עתידי — QR לא נקרא, ניקוי env vars, FK CASCADE חסר. הכותרת הראשית במסמך עודכנה ל-"גרסה 1.2 · 29 ביוני 2026". קובץ v1.0.docx הישן נשמר ב-Brief/ כהיסטוריה (אפשר למחוק כי git שומר את ההיסטוריה — נדחה להחלטה). | בוצע |
| 2026-06-29 | 🧹 | תפעול/אדמין | **MFA Authenticator הישן של A הוסר מהטלפון** | אחרי שלב 6 (`DELETE FROM A.auth.users WHERE id='a291ed14...'`), ה-factor של matkonadmin ב-A נמחק ב-cascade — הקוד הישן בטלפון ב-Google Authenticator/iCloud Keychain TOTP כבר לא תקף. המשתמש הסיר את הרשומה הישנה מהטלפון. נשארה רק ה-factor של matkonadmin ב-B (matkon.cloud, המאומת והפעיל). | ✅ הושלם |
| 2026-06-29 | ✅ | אבטחה/ארכיטקטורה | **הפרדת DB אדמין — שלב 6 הושלם, A נטוהר במלואו ב-7/7 שלבים** | אחרי כתיבת-audit חיה אומתה (פקד תקציב $50→$45 רשם שורה ב-`admin_actions_log` של B עם `admin_id=4bcbe223...`), בוצעו 5 DROPs בלתי-הפיכים ב-A: (1) `DELETE matkonadmin FROM auth.users` (UID `a291ed14...`) + 2 שורות יתומות מ-`public.users` (UIDs `97dc3e28` + `a291ed14`, שאריות ריקות מסשן 22.6); (2) `ALTER TABLE user_security DROP COLUMN role` (נדרש gating מקדים: 2 policies תלויות שנמחקו — `moderation_log_admin_read` + `usage_log_admin_read`, dead-weight בארכיטקטורה החדשה שעובדת רק דרך service_role); (3) `DROP FUNCTION admin_audit_recent(integer)`; (4) `DROP TABLE admin_actions_log`; (5) `DROP TABLE admin_bootstrap`. נשארו ב-A רק 2 RPCs לדשבורד (`admin_dashboard_flags` + `admin_dashboard_stats`) שנקראות מ-B דרך publicData — לפי התוכנית. **אומת חי:** matkon.cloud דשבורד עדיין טוען מלא + audit מציג 2 שורות (היסטורית + החדשה); matkon.co (ציבורי) עובד אחרי deploy `dpl_DrNFh36T` (commit `380e90e`, 10 שניות build); `GET https://matkon.cloud/api/audit\|stats\|config` ללא טוקן → 401. **תזכורת למשתמש:** ה-MFA Authenticator הישן של A תקף לא יותר (auth.users של A נמחק → factor נמחק עם זה) — אפשר להסיר מהטלפון. | ✅ הושלם |
| 2026-06-29 | 🐛 | אבטחה/ציבורי | **תיקון קוד ציבורי — הסרת קריאות `role` מ-`user_security`** (חוסם ל-DROP COLUMN בשלב 6) | בבדיקה לפני ניקוי A התגלה ש-4 קבצים באפליקציה הציבורית עדיין קוראים את העמודה `role` מ-`user_security` (שלא נמחק בסשן הפרדת ה-DB): `src/lib/AuthContext.jsx` (loadProfile), `api/publish-recipe.js`, `api/update-recipe.js`, `api/parse-recipe.js`. ה-`role` לא בשימוש בפועל (אומת ב-grep — אף `profile.role` בקוד ציבורי), אבל ה-`select=role,banned` בקריאות ל-PostgREST יחזיר 400 ברגע שהעמודה תיפול. תיקון: להסיר את `role` מכל ה-select-ים ולהסיר את `role: securityRow?.role` מהcontext. אומת `vite build` נקי. אם לא היה מתוקן — `matkon.co` היה נופל ב-login אחרי DROP COLUMN. | פרוס |
| 2026-06-29 | 🧹 | אבטחה/אדמין | **ניקוי A: matkonadmin היתום נמחק מ-auth.users + 2 שורות יתומות מ-public.users** (שלב 6 התחיל) | מחיקת `auth.users WHERE id='a291ed14...'` (matkonadmin הישן ב-A). cascade ניקה את `user_security`. **ממצא לוואי:** נמצאו 2 שורות יתומות ב-`public.users` (UIDs `97dc3e28` מ-22.6 02:09 ו-`a291ed14` מ-22.6 02:21 — שתיהן שורות ריקות בלי שם/מדינה, מצב לפני/אחרי שחזור סיסמת matkonadmin בסשן 22.6). נמחקו גם הן. **3 שורות seed יתומות** (מיכל/יוני/דנה — בעלי מתכוני seed בפיד) **לא נגעו** — בכוונה ככה. **ממצא רחב לעתיד:** אין FK עם `ON DELETE CASCADE` מ-`public.users` ל-`auth.users` (לכן השורות נשארו יתומות מלכתחילה) — תיקון schema לא דחוף. | בוצע |
| 2026-06-29 | 🔒 | אבטחה/ארכיטקטורה | **הפרדת DB אדמין — שלבים 1-5+7 הושלמו ומאומתים E2E** (ניקוי A דחוי) | **המבנה החדש פרוס וחי.** פרויקט Supabase B נפרד `matkon-admins` (id `luijrscftcsofqsznfiy`, Sydney) — 3 טבלאות `admin_user_security`/`admin_bootstrap`/`admin_actions_log` + 3 RPCs + 2 triggers + super_admin lock + RLS+REVOKE. matkonadmin@gmail.com נוצר ב-B (UID `4bcbe223...`), role=super_admin אוטומטית, total_super_admins=1 (דלת ננעלה). שורת audit היסטורית הועתקה. Auth של B הוגדר (MFA/OTP=900/Redirect=matkon.cloud). **קוד admin/ עודכן** ב-9 קבצים (commit `e1a7dcf` ב-main, deploy `dpl_CG3M7y` READY): `api/_supabase.js` rewrite (factory `makeClient`, dual exports `adminAuth`+`publicData`), `api/_admin.js` קורא admin_user_security מ-B, endpoints מנותבים נכון (audit→B, stats/flags/ai-cost/config→A, config.insert audit→B), `App.jsx` קליינט קורא admin_user_security. **env vars חדשים ב-Vercel** (Production): ADMIN_SUPABASE_URL/ANON/SERVICE_ROLE, PUBLIC_SUPABASE_URL; PUBLIC_SUPABASE_SERVICE_ROLE_KEY דולג (fallback ל-SUPABASE_SERVICE_ROLE_KEY הקיים — ניקוי עתידי ב-[[project_env_cleanup_post_split]]). VITE_SUPABASE_URL/ANON_KEY הוחלפו ל-B. **🎯 E2E חי אומת** ב-matkon.cloud: התחברות מצליחה, MFA enrollment חדש ל-B, דשבורד טוען עם נתוני A (7 משתמשים/4 פעילים/7 מתכונים/4 ציבוריים/קהילות/פופולריים/תגיות), AI cost בפועל $0.18 מ-Anthropic (סופר-אדמין רואה עיפרון עריכה), **שורת ה-audit ההיסטורית מופיעה ב-"פעולות מנהלים אחרונות" ע"י matkonadmin**. אזהרות `2 INFO advisors` על RLS-no-policy ב-admin_bootstrap+admin_actions_log = בכוונה (deny-all, service_role עוקף). **דחוי לסשן הבא:** שלב 6 ניקוי A (DROP user_security.role, admin_actions_log, admin_bootstrap, admin_audit_recent מ-A; מחיקת matkonadmin מ-A.auth.users) — בלתי הפיך. גם בדיקת UI לשינוי תקציב (audit write חדש) דחויה לפני המחיקה. | חי+מאומת (ניקוי A דחוי) |
| 2026-06-29 | ⏳ | אדמין/UX | **QR ב-MfaGate לא נקרא ע"י Authenticator (אומת פעמיים)** | בכניסה ראשונה למסך MFA enrollment של matkon.cloud, סריקת ה-QR ב-Google Authenticator/iCloud Keychain TOTP **נכשלת**. המשתמש נאלץ להעתיק ידנית את secret key המוצג. שורש משוער: Supabase מציג QR כ-SVG embedded ב-data URL (`data:image/svg+xml;utf-8,...`), שאפליקציות TOTP לא תומכות בו. אומת ב-22.6.26 (A) וב-29.6.26 (B). UX בעיה — לתקן לפני בניית UI ניהול אדמינים (אחרת כל אדמין חדש יחווה את זה). תיעוד פנימי: [[project_admin_qr_unreadable]]. | פתוח |
| 2026-06-29 | ⏳ | תפעול | **ניקוי deployment URLs ישנים ב-Vercel מתקופת "israelkitchen"** (דחוי לעתיד רחוק) | המשתמש זיהה URL ישן: `israelkitchen-2aemkcqhb-ynaftalis-projects.vercel.app`. בדיקה: בteam `ynaftalis-projects` יש רק 2 פרויקטים (`matkon`+`matkon-admin`), אין פרויקט "israelkitchen". זה preview URL שמור מתקופת שם ישן של הפרויקט. מוגן ב-Vercel Authentication (302→SSO), לא נגיש לציבור. דחוי לבקשת המשתמש — "עתיד רחוק". מתועד ב-`project_vercel_cleanup_old_urls.md`. | נדחה |
| 2026-06-28 | ⏳ | אדמין | **כלי "דוחות" — UI ידידותי לשאילתות ויצוא דוחות** | באדמין יש כיום רק קוביות סטטיסטיקה קבועות; לכל שאלה חדשה צריך להריץ SQL ידני דרך MCP. לבנות מסך שמאפשר לסופר-אדמין/אדמין לייצר דוחות בלי קוד — בחירת שדות, פילטרים (תאריך/מדינה/קטגוריה), טבלת תוצאות + ייצוא ל-CSV/Excel; ולמשתמש מתקדם — query builder עם dropdowns. דרישת משתמש 28.6. שלב ב'+ של הדשבורד, כנראה אחרי הפרדת ה-DB וניהול אדמינים. | פתוח |
| 2026-06-28 | 🔒 | משפטי/הרשמה | **יומן אישורי תקנון עם הוכחה משפטית (IP + UA + גרסה)** | צ׳ק-בוקס לבד שומר רק `tos_accepted_at` בעמודה אחת — לא מספיק להגנה משפטית. נוסף `tos_acceptance_log` (UUID/user_id/tos_version/ip_address inet/user_agent/source/accepted_at) + RLS self-read + `app_config.tos_current_version='v1.0'` (DB-driven, bump בלי deploy). endpoint שרת `/api/log-tos` מחלץ IP+UA מ-headers (הלקוח לא יכול לזייף); זהות נגזרת מטוקן או מ-user_id חדש (<5 דק') במקרה signup לפני אימות אימייל. Register+SSOCountry קוראים אחרי הסכמה (fire-and-forget — שגיאת רשת לא חוסמת signup). שאילתת שליפה: `SELECT * FROM tos_acceptance_log WHERE user_id=$1 ORDER BY accepted_at DESC`. migration `20260628150000_tos_acceptance_log.sql`. | פרוס |
| 2026-06-28 | ✨ | אבטחה/הרשמה | **צ׳ק-בוקס תנאי שימוש מפורש בהרשמה** (פער בריף 170) | בעמוד `/register` היה רק טקסט+קישור — אין הסכמה אקטיבית. נוסף checkbox חובה ("קראתי ואני מסכים/ה לתנאי השימוש"); הכפתור disabled עד שמסומן. ב-`signUp` עובר `tos_accepted_at: now()` ב-`raw_user_meta_data`. migration `20260628140000_handle_new_user_tos.sql` (הוחל): טריגר `handle_new_user` קולט עכשיו את ה-timestamp לעמודה `tos_accepted_at` (קיימת מאז SSO; משתמשים חדשים דרך אימייל יקבלו ערך). אומת preview: checkbox מופיע, RTL תקין, כפתור disabled מההתחלה, אין שגיאות קונסול. SSO Google כבר עובד אותו דבר (SSOCountry שומר tos_accepted_at). | פרוס |
| 2026-06-28 | ⏳ | אדמין | **ניהול אדמינים (UI) — נדחה עד שתבוצע הפרדת DB** | מסך ניהול האדמינים (רשימה / הזמנה / שינוי רמה / איפוס סיסמה / חסימה / הסרת הרשאות / מחיקה מלאה) ייבנה רק אחרי הפרדת ה-DB. החלטה: לבנות אותו פעם אחת על הארכיטקטורה הנכונה במקום לבנות פעמיים. סקופ מוסכם כשנגיע: שתי פעולות "הסרה" — `permissions.revoke` (הורדה ל-user, הפיך) ו-`account.delete` (מחיקה מלאה עם אישור-אימייל כפול); super_admin לא יכול לפעול על עצמו או על super_admin אחר דרך UI. | נדחה |
| 2026-06-28 | 🧭 | אבטחה/ארכיטקטורה | **הפרדת DB מלאה לאדמין — שני פרויקטי Supabase** | זוהה ע"י המשתמש: כיום האדמין והאפליקציה הציבורית חולקים את אותו פרויקט Supabase, וההפרדה היחידה היא עמודת `role`. החלטה: לעבור ל-2 פרויקטים נפרדים — auth של האדמינים יושב ב-DB משלו, פעולות על נתוני האפליקציה הציבורית עוברות דרך service-role API חיצוני. עבודה משוערת ~3-4 שעות, סשן ייעודי (לא בסשן קצרצר). תוכנית מלאה ב-`project_matkon_status.md`. **לבצע לפני** מסך ניהול האדמינים. | פתוח |
| 2026-06-28 | ✨ | אדמין | **דשבורד שלב ב' — סקשן "פעולות מנהלים אחרונות"** | סוגר את הלולאה לפקד התקציב (כותב audit מ-25.6 בלי איפה לראות). RPC `admin_audit_recent` (SECURITY DEFINER, JOIN ל-users+auth.users, EXECUTE שלול מ-public/anon/authenticated). endpoint `admin/api/audit.js` עם cap חדש `audit.view` (admin+super_admin בלבד; moderator לא). סקשן בדשבורד עם מיפוי ידידותי לעברית (`budget.update` → "עדכון תקרת תקציב AI · $X → $Y"). **אומת חי:** RPC מחזיר את שורת ה-budget.update הקיימת ($25→$50, ע"י matkonadmin); admin build נקי; commit `cd0a5f0` ב-Vercel READY (`dpl_9duvXV4`, 6 functions עם audit); `GET https://matkon.cloud/api/audit` ללא טוקן → 401 "נדרשת התחברות" (Authn-gate פעיל). תצוגת ה-UI עצמה דורשת כניסת אדמין+MFA כדי לוודא. | פרוס |
| 2026-06-25 | ✨ | עלויות AI | **עלות AI אמת — חיבור Anthropic Usage & Cost API** | מחליף את ההערכה בעלות שחויבה בפועל. `admin/api/_anthropic.js` קורא ל-`/v1/organizations/cost_report` (x-api-key=מפתח Admin, סכומים באגורות→$/100, חלון MTD+30 יום+פירוט לפי מודל) ושומר snapshot ל-`app_config.ai_real_spend`. `admin/api/ai-cost.js` (GET, stats.view+aal2) + cron יומי `admin/api/cron/sync-ai-cost.js` (מוגן ב-CRON_SECRET). migration: `ai_budget_status()` מעדיף עלות אמת (+הערכת טוקנים לזנב שאחרי ה-snapshot, לעולם לא תת-ספירה), נופל להערכה אם אין snapshot טרי. הדשבורד מציג עלות אמת ככותרת + תיוג "הערכה" על הפירוט. **🎯 אומת E2E חי**: matkon.cloud מציג $0.16 חיוב בפועל (תואם 1:1 ל-Anthropic Console) עם פירוט per-model (haiku $0.13 + sonnet $0.02). 🔥 **ההערכה לפי טוקנים הייתה $0.0223 — פי-7 פחות מהאמת**, בדיוק הסיבה שעשינו את זה. cron יומי 03:00 UTC. commit `3ea9d22` + redeploy `R9mFM8sk`. | פרוס |
| 2026-06-25 | 🧭 | עלויות AI | **תקרת Anthropic ↔ תקרת דשבורד — buffer של $5** | החלטת משתמש לאחר אימות הבלם: Anthropic spend limit הורד מ-$500 ל-**$50**, ותקרת הדשבורד הורדה מ-$50 ל-**$45** דרך פקד העיפרון. הסיבה: כשהשתיים שוות, Anthropic יכול לחתוך קריאה ב-429 גולמי לפני שהבלם שלנו מגיע ל-100%. **כלל**: דשבורד < Anthropic, פער ~$5 buffer | בוצע |
| 2026-06-25 | ✨ | אדמין | **דשבורד שלב ב' התחיל — פקד עריכת תקרת תקציב AI** | פעולת-הכתיבה הראשונה באדמין; מקימה את התבנית לכל פעולות שלב ב' (token+MFA aal2+role+audit). `api/config.js` (super_admin בלבד דרך `budget.manage`, ולידציה 1–150, רישום ל-`admin_actions_log`), helpers `adminUpdate`/`adminInsert`, פקד עיפרון בדשבורד + מצב עריכה inline. commit `bb2ce27`. **אומת E2E חי**: שינוי $25→$50 נשמר ב-DB + נרשמה שורת audit (`from:25,to:50`, ע"י matkonadmin) | פרוס |
| 2026-06-25 | ✅ | DevOps | **גישת Vercel MCP לאדמין — נפתר** (משימה 25.6) | התגלה שיש **שני** חיבורי Vercel MCP: `mcp__plugin_vercel` מוגבל לפרויקט הציבורי (403 על האדמין), אבל `mcp__d23633aa...` **רואה ומנהל את matkon-admin** (deployments, logs, web_fetch שעוקף edge-cache). מעכשיו אפשר לאמת deploys של האדמין מהצ'אט | בוצע |
| 2026-06-25 | 🐛 | עלויות AI | **"שעון ה-AI" בדשבורד מבוסס הערכה, לא נתון אמת** — התגלה ע"י המשתמש | עלות הדולרים = טוקנים (אמת) × טבלת מחירים מקומית (הערכה; סוטה בגלל prompt caching/שינויי מחירון). שאר הדשבורד = נתוני אמת מה-DB. **כלל מחודש: נתוני אמת תמיד, ולהציף כל החלטת אמת-מול-הערכה.** לתיקון בסשן ייעודי (סעיף הבא) | פתוח |
| 2026-06-25 | ⏳ | עלויות AI | **חיבור Usage & Cost API של Anthropic — עלות אמת** | להחליף את ההערכה בעלות שחויבה בפועל (חלון 30 יום + מתחילת החודש); הבלם יעצור לפי הוצאה אמיתית. דורש מפתח Admin (`sk-ant-admin`, server-side באדמין) שהמשתמש ייצר. סשן ייעודי | פתוח |
| 2026-06-25 | 🧭 | עלויות AI | **תקרת ההוצאה של Anthropic לא ניתנת לשינוי דרך API** (אומת בתיעוד) | spend limit נקבע **רק ידנית ב-Console** (Rate Limits/Usage APIs הם קריאה בלבד; Admin API לא חושף הגדרת תקרה). מסקנה: הגדלת מכסה מעבר לחשבון = קודם ידנית ב-Anthropic, ואז בדשבורד. תקרת הדשבורד צריכה להיות ≤ תקרת Anthropic כדי שהבלם יעצור יפה לפני חיתוך גולמי | בוצע |
| 2026-06-24 | ✨ | עלויות AI | **תקרת תקציב AI חודשית — אכיפה + התראה** (P0 בריף) | תקרה ב-`app_config` (ברירת מחדל $25, ניתנת לשינוי בלי deploy דרך SQL); RPC `ai_budget_status` (עלות-מתחילת-החודש מול תקרה); עצירה קשיחה ב-100% (`parse-recipe`+`translate-ingredients` מחזירים 503 גנרי) + warning ב-80% ב-Vercel logs; בלוק תקציב בדשבורד (בר צבעוני + התראה). החלטת משתמש: עצירה קשיחה + סף רך 80% | פרוס |
| 2026-06-24 | 🔒 | אבטחה/עלויות | **חסימת קריאות AI אנונימיות** ב-`parse-recipe` ו-`translate-ingredients` (401 ללא טוקן תקף) + route guard ל-`/add` | אורח יכל ל-POST ישירות לשרת ולשרוף טוקנים ללא הגבלה (תמונה→Sonnet, יקר); `translate` אף סמך על `userId` מגוף הבקשה — הפרת כלל אבטחה (לא לסמוך על מזהה מהלקוח). החלטת משתמש: דרך א' (לחסום לא-מחוברים). זהות נגזרת רק מהטוקן. אומת חי: POST אנונימי→401 | פרוס |
| 2026-06-24 | ⏳ | מודרציה | **מודרציה לתמונות שנוצרות (Pollinations)** — תמונת ה-AI לא עוברת בדיקת vision | פער בריף "גבוה": התמונה נוצרת *אחרי* המודרציה (publish-recipe שורה 75). סיכון מעשי נמוך (prompt נשלט לאוכל) אך דרישת בריף. דורש החלטה: עלות+latency של vision לכל פרסום מול הסיכון | פתוח |
| 2026-06-24 | ⏳ | מתכון | **מציאת מצרכים נדירים — web search אמיתי** | פער בריף P1: `translate-ingredients` רק *מנחש* `where_to_buy` כטקסט מהמודל. חסר חיפוש רשת אמיתי שמחזיר 2-3 חנויות עם לינקים קליקביליים + cache | פתוח |
| 2026-06-24 | ⏳ | תשתית | **חלופת ספק תמונות** ל-Pollinations | פער בריף P2: רק Pollinations (שירות חינמי ציבורי), אין fallback אם נופל | פתוח |
| 2026-06-24 | ⏳ | אדמין | **דשבורד שלב ב'** — פעולות ניהול | dashboard doc §11: ניהול אדמינים, טבלאות עריכה, חסימה/מחיקה/הסתרה/איפוס סיסמה, **+ פקד עריכת תקרת התקציב** (במקום SQL). כולן פעולות-כתיבה הדורשות MFA+role+audit log | פתוח |
| 2026-06-24 | ⏳ | DevOps | **גישת Vercel MCP לפרויקט האדמין** (משימה 25.6) | חיבור ה-MCP מוגבל לפרויקט הציבורי בלבד → אי-אפשר לצפות/לאמת deploys של matkon.cloud דרך הכלי. להרחיב את ה-scope | פתוח |
| 2026-06-23 | ✨ | העלאת מתכון | **עריכה מלאה בשלב 3** (כותרת/תיאור/מרכיבים/שלבים/זמנים/רמה) לפני שמירה | פער מול הבריף ("למשתמש תמיד יש אפשרות לערוך לאחר תוצר ה-AI"); המשתמש לא יכול היה לתקן זיהוי שגוי | פרוס |
| 2026-06-23 | ✨ | זיהוי תמונה | **Sonnet לחילוץ מתמונה** (טקסט/לינק נשארים Haiku) | Haiku קרא שגוי צילום נאגטס→"פאסטה". Sonnet = זיהוי הרבה יותר מדויק; עלות גבוהה רק לקלט-תמונה. החלטת משתמש (HUMAN AUTHORITY) | פרוס |
| 2026-06-23 | 🐛 | זיהוי | תיקון כתיב ("פסטה" לא "פאסטה") + הנחיה לא לשנות סוג מנה/להמציא מרכיבים מתמונה | דיוק נאמן למקור | פרוס |
| 2026-06-23 | 🐛 | תמונה/עלויות | תמונת המנה האוטומטית נוצרת **רק אחרי** שהמתכון עבר ונשמר (server-side); הוסרה תצוגת ה-preview בשלב 3 | מנע בזבוז יצירת תמונה על מתכון שנחסם (כמו הפאסטה). תמונת משתמש עדיין נבדקת לפני פרסום | פרוס |
| 2026-06-23 | ✨ | UX | שיפוץ עמוד "המתכון נשמר" (שלב 4): תמונת המנה למעלה, שם המתכון בירוק בולט מתחת לתמונה, הוסר אימוג'י 🎉, כפתורי שיתוף פעילים (וואטסאפ/פייסבוק/X + העתקת לינק), ו"בחזרה לעמוד הראשי" ככפתור | הדף היה ריק/ילדותי; עכשיו "חי" ופונקציונלי. אינסטגרם הוסר (אין שיתוף-web) | פרוס |
| 2026-06-23 | 🐛 | מודרציה | שיפוץ מנוע המודרציה לארכיטקטורת **2 שערים** | מתכון לגיטימי (פפריקה+מים) נחסם והמשתמש צבר strikes על טקסט שה-AI עצמו המציא. עיקרון חדש: strike רק על תוכן פוגעני שהמשתמש העלה; AI-generated content חוסם אך לעולם לא מעניש | פרוס |
| 2026-06-23 | ✨ | מודרציה | **שתי ספירות נפרדות**: פוגעני→חסימה ב-3, זיבול→חסימה ב-5 + דה-דופ 24ש' | הפרדה בין תוכן פוגעני (חמור) לזיבול/בוטים; דה-דופ מונע ערימת strikes על double-click | פרוס |
| 2026-06-23 | ✨ | מודרציה | תקרת פרסום **20 מתכונים/יום** למשתמש (hard block) | אנטי-הצפה/בוטים. התראה רכה ב-10 → תיבנה בדשבורד (שלב ב') | פרוס |
| 2026-06-23 | ✨ | UX | כרטיס חסימה חדש: ציטוט הביטוי הפוגעני (לתיקון שוגג), כתום לזיבול, כפתורי "עריכה ותיקון"/"לעמוד הראשי" | המשתמש צריך לדעת *איפה* טעה כדי לתקן | פרוס |
| 2026-06-23 | 🐛 | העלאת מתכון | תיקון כשל בהעלאת תמונה כמתכון ("שגיאה בשירות ה-AI") | צילומי מסך iPhone הם PNG אך נשלחו עם `media_type:'image/jpeg'`; עכשיו מנורמלים ל-JPEG דחוס לפני שליחה ל-AI | פרוס |
| 2026-06-23 | 🐛 | DevOps | git remote תוקן לכתובת קנונית (`MATKON.git`) | ה-remote הקודם (`matkon.git`) עשה redirect ש**מנע טריגר deploy ב-Vercel** למרות ש-push הצליח ל-GitHub | פרוס |
| 2026-06-23 | 🧭 | תיעוד | הוחלט לנהל `CHANGELOG.md` ב-repo + ייצוא Excel לפי דרישה | best practice: טקסט מנוהל-גרסאות, בלי drift; אקסל נגזר לפי צורך | בוצע |
| 2026-06-23 | ⏳ | עיצוב | שדרוג עיצוב כללי (כולל עמוד השיתוף) — סבב פוליש עתידי | המשתמש אישר את המצב הנוכחי כ"בסדר בינתיים" | נדחה |
| 2026-06-23 | ⏳ | Auth | **Passkeys / Face ID** לאפליקציה הציבורית | המשתמש נדרש להזין סיסמה כל כניסה — כנראה iOS מוחק localStorage (לא באג). passkeys = פתרון מאובטח+נוח, פיצ'ר לבנות | פתוח |
| 2026-06-23 | ⏳ | עלויות AI | `parse-recipe` של אורח לא-מחובר ללא rate-limit | חור עלות פוטנציאלי — **נסגר 24.6** (דרישת אימות 401 + תקרת תקציב) | פרוס |
| 2026-06-23 | ⏳ | מוצר | תקרות פרסום ומכסות AI כפרמטר פר-tier למודל מנויים בתשלום | היום התקרה אנטי-בוט; בעתיד כלי תמחור (free מול premium) | נדחה |
| 2026-06-22 | ✨ | אדמין | פריסת אפליקציית האדמין ל-**matkon.cloud** הושלמה ואומתה E2E | הפרדת רשויות מלאה; התחברות+MFA+role+RPCs עובדים | פרוס |
| 2026-06-22 | 🔒 | אדמין | הקשחת session: idle 30 דק' + תקרה מוחלטת 8 שעות | החלטת "מאוזן" של המשתמש | פרוס |
| 2026-06-22 | 🐛 | אבטחה | תיקון רקורסיה אינסופית ב-RLS של `user_security` | קריאת role נכשלה→null; תיקן גם את האפליקציה הציבורית | פרוס |
| 2026-06-22 | 🐛 | אדמין | forgot-password עם MFA דרש step-up ל-aal2 לפני שינוי סיסמה | לינק שחזור יוצר סשן aal1; Supabase דורש aal2 לשינוי סיסמה | פרוס |
| 2026-06-19 | ✨ | אדמין | אפליקציית אדמין נפרדת + MFA (TOTP) + דרגת super_admin | רמת אבטחה מקסימלית; קוד אדמין לא ב-bundle הציבורי | פרוס |
| 2026-06-19 | ✨ | אדמין | דשבורד שלב א' (read-only): סטטיסטיקות, דגלים אדומים, עלויות AI | ניהול המערכת | פרוס |
| 2026-06-19 | 🔒 | פרטיות | הסרת אווטרים לחלוטין (דגל+שם פרטי בלבד) | ISRAEL ATTENTION — לא לחשוף פרטים מזהים של ישראלים בחו"ל | פרוס |
| 2026-06-19 | ✨ | Auth | SSO גוגל חי ואומת E2E (אפל מאחורי flag עד חשבון בתשלום) | סגירת פער בריף P0 | פרוס |
| 2026-06-18 | ✨ | מתכונים | עריכה + מחיקה לצמיתות ע"י הבעלים (אישור כפול) | פער ליבה מהבריף | פרוס |
| 2026-06-18 | 🔒 | אבטחה/GDPR | פיצול `user_security`, מתכונים private כברירת מחדל, ייצוא+מחיקת חשבון, גיבוי DB יומי | יישום הנחיות האבטחה וה-GDPR | פרוס |
| 2026-06-16 | 🔒 | מודרציה | שער מודרציה שרת-צד (גרסה 1) + 3-strikes; Feed/Recipes מ-Supabase | דרישת חובה מהבריף | פרוס |
| 2026-06-03 | ✨ | תשתית | הקמת הפרויקט: React+Vite, Supabase, Vercel, סכמה ראשונית | — | פרוס |
